{
  "targets": {
    "build": {
      "dependsOn": ["restore", "^build"],
      "defaultConfiguration": "release"
    },
    "test": {
      "dependsOn": ["build"],
      "configurations": {
        "release": {
          "args": ["--no-build", "--no-restore", "--configuration", "Release"]
        }
      },
      "defaultConfiguration": "release"
    }
  }
}
