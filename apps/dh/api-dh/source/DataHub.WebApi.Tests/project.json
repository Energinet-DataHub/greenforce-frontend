{
  "name": "api-dh-tests",
  "$schema": "../../../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/dh/api-dh/source/DataHub.WebApi.Tests",
  "prefix": "dh",
  "targets": {
    "test": {
      "options": {
        "configuration": "Release",
        "filter": "DisplayName!~TelemetryTests",
        "logger": "console;verbosity=detailed"
      }
    },
    "telemetry": {
      "executor": "@nx-dotnet/core:test",
      "options": {
        "noBuild": true,
        "configuration": "Release",
        "filter": "DisplayName~TelemetryTests",
        "logger": "console;verbosity=detailed"
      }
    }
  },
  "tags": ["product:dh", "type:test-util"]
}
