{
  "name": "eov-shared-environments",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/eov/shared/environments/src",
  "prefix": "eov",
  "tags": ["product:eov", "domain:shared", "type:environments"],
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/eov/shared/environments/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "libs/eov/shared/environments/**/*.ts",
          "libs/eov/shared/environments/**/*.html"
        ]
      }
    }
  }
}
