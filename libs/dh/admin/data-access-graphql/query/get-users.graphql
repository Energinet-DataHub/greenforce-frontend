query GetUsers(
  $actorId: UUID
  $userRoleIds: [UUID!]
  $userStatus: [UserStatus!]
  $first: Int
  $last: Int
  $after: String
  $before: String
  $order: UsersSortInput
  $filter: String
) {
  users(
    actorId: $actorId
    userRoleIds: $userRoleIds
    userStatus: $userStatus
    first: $first
    last: $last
    before: $before
    after: $after
    order: $order
    filter: $filter
  ) {
    nodes {
      name
      email
      phoneNumber
      status
      id
      latestLoginAt
    }
    totalCount
    pageInfo {
      startCursor
      endCursor
    }
  }
}
