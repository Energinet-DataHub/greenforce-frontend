query GetPermissionDetails($id: Int!) {
  permissionById(id: $id) {
    id
    name
    description
    created
    assignableTo
    userRoles {
      id
      name
      description
      eicFunction
      status
    }
  }
}
