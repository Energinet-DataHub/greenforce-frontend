query GetBalanceResponsibleRelation($id: UUID!) {
  marketParticipantById(id: $id) {
    id
    balanceResponsibleAgreements {
      validPeriod
      status
      gridArea {
        displayCodeGlnActor
        id
        code
      }
      meteringPointType
      balanceResponsibleWithName {
        id
        displayNameWithoutMarketRole
      }
      energySupplierWithName {
        id
        displayNameWithoutMarketRole
      }
    }
  }
}
