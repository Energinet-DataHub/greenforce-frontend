query GetMarketParticipantsByOrganizationId($organizationId: UUID!) {
  marketParticipantsByOrganizationId(organizationId: $organizationId) {
    id
    glnOrEicNumber
    name
    marketRole
    status
  }
}
