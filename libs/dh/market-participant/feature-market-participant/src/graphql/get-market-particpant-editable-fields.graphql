query GetMarketParticipantEditableFields($marketParticipantId: UUID!) {
  marketParticipantById(id: $marketParticipantId) {
    id
    name
    organization {
      domains
      id
    }
    contact {
      name
      email
      phone
    }
  }
}
