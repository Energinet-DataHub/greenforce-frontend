query GetActorEditableFields($actorId: UUID!) {
  actorById(id: $actorId) {
    id
    name
    organization {
      domains
      id
    }
    contact {
      name
      email
      phone
    }
  }
}
