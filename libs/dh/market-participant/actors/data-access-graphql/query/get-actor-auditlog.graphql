query GetActorAuditLogs($id: UUID!) {
  actorById(id: $id) {
    id
    auditLog {
      auditedBy
      change
      timestamp
      isInitialAssignment
      currentValue
      previousValue
      consolidation {
        currentOwner
        previousOwner
        currentGridAreaName
        previousGridAreaName
        previousGridAreaStopsAt
      }
      delagation {
        actor
        gln
        startsAt
        stopsAt
        gridAreaName
        processType
      }
    }
  }
}
