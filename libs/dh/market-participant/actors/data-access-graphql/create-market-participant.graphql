mutation CreateMarketParticipant($input: CreateMarketParticipantInput!) {
  createMarketParticipant(input: $input) {
    errors {
      ... on ApiError {
        apiErrors {
          ...
        }
      }
    }
    success: boolean
  }
}
