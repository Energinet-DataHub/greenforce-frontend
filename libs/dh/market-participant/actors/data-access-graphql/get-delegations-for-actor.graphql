query GetDelegationsForActor($actorId: UUID!) {
  getDelegationsForActor(actorId: $actorId) {
    periodId
    id
    messageType
    delegatedBy {
      id
      name
    }
    delegatedTo {
      id
      name
    }
    validPeriod
    gridArea {
      id
      code
    }
    status
  }
}
