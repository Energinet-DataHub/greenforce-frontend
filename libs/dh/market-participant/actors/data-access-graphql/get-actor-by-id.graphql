query GetActorById($id: UUID!) {
  actorById(id: $id) {
    id
    glnOrEicNumber
    name
    marketRole
    status
    gridAreas {
      code
      name
      displayName
      id
    }
    organization {
      name
    }
  }
}
