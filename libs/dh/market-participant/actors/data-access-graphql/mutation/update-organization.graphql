mutation UpdateOrganization($input: UpdateOrganizationInput!) {
  updateOrganization(input: $input) {
    boolean
    errors {
      ... on ApiError {
        apiErrors {
          message
          code
          args
        }
      }
    }
  }
}
