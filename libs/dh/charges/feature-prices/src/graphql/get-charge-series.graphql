query GetChargeSeries($chargeId: UUID!, $interval: DateRange!) {
  chargeSeries(chargeId: $chargeId, interval: $interval) {
    totalAmount
    points {
      price
      fromDateTime
      toDateTime
    }
  }
}
