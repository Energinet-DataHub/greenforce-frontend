query GetChargeSeries($chargeId: String!, $interval: DateRange!) {
  chargeById(id: $chargeId) {
    ...ChargeFields
    series(interval: $interval) {
      period
      price
      hasChanged
      points {
        isCurrent
        price
        fromDateTime
        toDateTime
      }
    }
  }
}
