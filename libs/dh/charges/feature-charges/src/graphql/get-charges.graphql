query GetCharges(
  $query: GetChargesQueryInput
  $skip: Int
  $take: Int
  $filter: String
  $order: ChargeSortInput
) {
  charges(query: $query, skip: $skip, take: $take, filter: $filter, order: $order) {
    totalCount
    items {
      ...ChargeFields
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
    }
  }
}
