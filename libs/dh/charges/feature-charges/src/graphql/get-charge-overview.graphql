query GetChargeOverview(
  $after: String
  $before: String
  $first: Int
  $last: Int
  $query: ChargeOverviewQueryInput
  $order: [ChargeOverviewItemSortInput!]
  $filter: String
) {
  chargeOverview(
    after: $after
    before: $before
    first: $first
    last: $last
    query: $query
    order: $order
    filter: $filter
  ) {
    totalCount
    nodes {
      period
      charge {
        ...ChargeFields
      }
    }
    pageInfo {
      startCursor
      endCursor
    }
  }
}
