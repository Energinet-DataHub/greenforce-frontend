query GetAggregatedMeasurementsForAllYears($meteringPointId: String!) {
  aggregatedMeasurementsForAllYears(query: { meteringPointId: $meteringPointId }) {
    quantity
    quality
    year
    unit
  }
}
