query GetContactCPR(
  $meteringPointId: String!
  $contactId: String!
  $searchMigratedMeteringPoints: Boolean!
) {
  meteringPointContactCpr(meteringPointId: $meteringPointId, contactId: $contactId, searchMigratedMeteringPoints: $searchMigratedMeteringPoints) {
    result
  }
}
