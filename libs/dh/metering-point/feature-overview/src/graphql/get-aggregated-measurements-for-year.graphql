query GetAggregatedMeasurementsForYear($meteringPointId: String!, $year: Int!) {
  aggregatedMeasurementsForYear(query: { meteringPointId: $meteringPointId, year: $year }) {
    yearMonth
    quantity
  }
}
