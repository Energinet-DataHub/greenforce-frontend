mutation AcceptRollbackChangeOfSupplier($meteringPointId: String!, $processId: UUID!) {
  acceptRollbackChangeOfSupplier(
    input: { meteringPointId: $meteringPointId, processId: $processId }
  ) {
    boolean
  }
}
