subscription OnMeteringPointProcessUpdated($meteringPointId: String!, $created: DateRange!) {
  meteringPointProcessUpdated(meteringPointId: $meteringPointId, created: $created) {
    id
    createdAt
    cutoffDate
    businessReason
    state
    initiator {
      id
      displayName
    }
    availableActions
  }
}
