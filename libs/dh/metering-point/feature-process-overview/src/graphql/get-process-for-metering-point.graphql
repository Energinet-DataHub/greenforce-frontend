query GetMeteringPointProcessById($id: String!) {
  meteringPointProcessById(id: $id) {
    id
    createdAt
    cutoffDate
    businessReason
    state
    availableActions
    cancellationTimestamp
    cancelledByProcess {
      id
      businessReason
      cutoffDate
    }
    steps {
      id
      step
      comment
      description
      completedAt
      dueDate

      actor {
        id
        displayName
      }
      actorRole
      state
      documentUrl
    }
    initiator {
      id
      displayName
      glnOrEicNumber
    }
    initiatorRole
  }
}
