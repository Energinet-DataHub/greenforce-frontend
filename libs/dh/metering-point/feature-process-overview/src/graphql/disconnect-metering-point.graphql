mutation DisconnectMeteringPoint($meteringPointId: String!, $validityDate: DateTime!) {
  disconnectMeteringPoint(
    input: { meteringPointId: $meteringPointId, validityDate: $validityDate }
  ) {
    boolean
  }
}
