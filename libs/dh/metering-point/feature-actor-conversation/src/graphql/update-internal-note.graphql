mutation UpdateInternalConversationNote($conversationId: UUID!, $internalNote: String!) {
  updateInternalConversationNote(
    updateInternalConversationNoteInput: {
      conversationId: $conversationId
      internalNote: $internalNote
    }
  ) {
    boolean
  }
}
