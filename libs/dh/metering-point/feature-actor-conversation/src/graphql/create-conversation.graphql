mutation CreateConversation($meteringPointIdentification: String!, $conversationMessageContent: String!) {
  createConversation(input: { meteringPointIdentification: $meteringPointIdentification, conversationMessageContent: $conversationMessageContent }) {
    boolean: boolean
  }
}
