query GetConversations($meteringPointIdentification: String!) {
  conversationsForMeteringPoint(meteringPointIdentification: $meteringPointIdentification) {
    conversations {
      id
      read
      subject
      closed
      lastUpdated
    }
  }
}
