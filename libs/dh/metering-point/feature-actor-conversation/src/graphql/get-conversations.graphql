query GetConversations($meteringPointIdentification: String!, $searchTerm: String) {
  conversationsForMeteringPoint(meteringPointIdentification: $meteringPointIdentification, searchTerm: $searchTerm) {
    conversations {
      id
      read
      subject
      displayId
      closed
      lastUpdated
    }
  }
}
