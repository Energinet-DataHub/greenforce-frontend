query DoesInternalMeteringPointIdExist($internalMeteringPointId: Long, $meteringPointId: Long) {
  meteringPointExists(
    internalMeteringPointId: $internalMeteringPointId
    meteringPointId: $meteringPointId
  ) {
    id
    meteringPointId
  }
}
