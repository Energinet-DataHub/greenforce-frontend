query DoesMeteringPointExist($meteringPointId: String!, $enableNewSecurityModel: Boolean) {
  meteringPoint(
    meteringPointId: $meteringPointId
    enableNewSecurityModel: $enableNewSecurityModel
  ) {
    id
    meteringPointId
  }
}
