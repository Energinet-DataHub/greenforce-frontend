query GetMeteringPointEventsDebugView($meteringPointId: String!) {
  eventsDebugView(meteringPointId: $meteringPointId) {
    meteringPointJson
    events {
      id
      timestamp
      type
      dataJson
    }
  }
}
