query GetMeteringPointUploadMetadataById($meteringPointId: String!) {
  meteringPoint(meteringPointId: $meteringPointId) {
    id
    metadata {
      id
      measureUnit
      resolution
      type
      subType
    }
  }
}
