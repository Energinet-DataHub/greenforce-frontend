query GetTemporaryStorageData($meteringPointId: String!, $transactionId: String!) {
  temporaryStorageData(meteringPointId: $meteringPointId, transactionId: $transactionId) {
    firstCustomerName
    isBusinessCustomer
  }
}
