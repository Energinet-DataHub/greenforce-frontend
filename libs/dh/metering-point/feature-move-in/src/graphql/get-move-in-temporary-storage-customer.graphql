query GetTemporaryStorageData($meteringPointId: String!, $processId: String!) {
  temporaryStorageData(meteringPointId: $meteringPointId, processId: $processId) {
    firstCustomerName
    isBusinessCustomer
  }
}
