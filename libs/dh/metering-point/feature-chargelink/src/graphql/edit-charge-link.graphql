mutation EditChargeLink($chargeLinkId: String!, $newStartDate: DateTime!, $factor: Int!) {
  editChargeLink(
    input: { chargeLinkId: $chargeLinkId, newStartDate: $newStartDate, factor: $factor }
  ) {
    success: boolean
  }
}
