mutation EditChargeLink(
  $chargeId: String!
  $meteringPointId: String!
  $newStartDate: DateTime!
  $factor: Int!
) {
  editChargeLink(
    input: {
      chargeId: $chargeId
      meteringPointId: $meteringPointId
      newStartDate: $newStartDate
      factor: $factor
    }
  ) {
    success: boolean
  }
}
