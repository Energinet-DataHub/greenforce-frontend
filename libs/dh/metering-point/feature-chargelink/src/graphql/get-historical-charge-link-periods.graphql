query GetHistoricalChargeLinkPeriods($id: String!) {
  chargeLinkPeriodById(id: $id) {
    id
    changes {
      created
      changeType
      stopDate
      factor
      previousFactor
    }
  }
}
