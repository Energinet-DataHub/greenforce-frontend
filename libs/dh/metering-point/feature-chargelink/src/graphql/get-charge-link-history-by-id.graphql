query GetChargeLinkHistory($id: String!) {
  chargeLinkById(id: $id) {
    id
    charge {
      id
      displayName
      owner {
        id
        displayName
      }
    }
    period {
      interval
    }
    history {
      submittedAt
      description
      messageId
    }
  }
}
