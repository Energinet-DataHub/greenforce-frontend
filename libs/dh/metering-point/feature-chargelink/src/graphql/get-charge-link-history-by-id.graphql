query GetChargeLinkHistory($meteringPointId: String!, $chargeLinkId: Long!) {
  chargeLinkById(meteringPointId: $meteringPointId, chargeLinkId: $chargeLinkId) {
    id
    charge {
      id
      owner {
        id
        displayName
      }
    }
    history {
      submittedAt
      description
      messageId
    }
  }
}
