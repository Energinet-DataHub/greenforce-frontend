query GetChargeLinkHistory($id: String!) {
  chargeLinkById(id: $id) {
    id
    charge {
      id
      displayName
      owner {
        id
        displayName
      }
    }
    history {
      submittedAt
      description
      messageId
    }
  }
}
