query GetChargeLinkById($chargeId: String!, $meteringPointId: String!) {
  chargeLinkById(chargeId: $chargeId, meteringPointId: $meteringPointId) {
    id
    charge {
      id
      displayName
    }
  }
}
