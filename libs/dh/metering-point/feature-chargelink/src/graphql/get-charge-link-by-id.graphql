query GetChargeLinkById($chargeLinkId: Long!, $meteringPointId: String!) {
  chargeLinkById(chargeLinkId: $chargeLinkId, meteringPointId: $meteringPointId) {
    id
    charge {
      id
      displayName
    }
  }
}
