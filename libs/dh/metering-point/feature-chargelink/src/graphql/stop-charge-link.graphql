mutation StopChargeLink($chargeId: String!, $meteringPointId: String!, $stopDate: DateTime!) {
  stopChargeLink(
    input: { chargeId: $chargeId, stopDate: $stopDate, meteringPointId: $meteringPointId }
  ) {
    success: boolean
  }
}
