query GetChargeLinkPeriodById($id: String!) {
  chargeLinkPeriodById(id: $id) {
    ...ChargeLinkPeriodFields
    changes {
      created
      changeType
      effectiveDate
      factor
      previousFactor
    }
  }
}
