query GetActorsForEicFunction($eicFunctions: [EicFunction!]) {
  actorsForEicFunction(eicFunctions: $eicFunctions) {
    id
    glnOrEicNumber
    name
    marketRole
    status
    gridAreas {
      id
      code
    }
  }
}
