query GetRelevantGridAreas($actorId: UUID, $period: PeriodInput!) {
  relevantGridAreas(actorId: $actorId, period: $period) {
    id
    code
    name
    displayName
    validTo
    validFrom
    includedInCalculation
    type
  }
}
