query GetActorMessageQueues($actorNumber: String!, $actorRole: String!) {
  actorMessageQueues(actorNumber: $actorNumber, actorRole: $actorRole) {
    queues {
      category
      count
      messages {
        messageId
        documentType
        businessReason
        enqueuedAt
      }
    }
  }
}
