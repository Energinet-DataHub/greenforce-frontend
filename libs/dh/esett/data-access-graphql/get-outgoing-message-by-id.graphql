query GetOutgoingMessageById($documentId: String!) {
  esettExchangeEventById(documentId: $documentId) {
    documentId
    processType
    created
    periodFrom
    periodTo
    documentStatus
    gridArea
    timeSeriesType
  }
}
