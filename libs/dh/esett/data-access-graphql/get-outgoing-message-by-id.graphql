query GetOutgoingMessageById($documentId: String!) {
  esettOutgoingMessageById(documentId: $documentId) {
    documentId
    calculationType
    created
    periodFrom
    periodTo
    documentStatus
    gridArea {
      displayName
    }
    timeSeriesType
  }
}
