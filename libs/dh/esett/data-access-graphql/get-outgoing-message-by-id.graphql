query GetOutgoingMessageById($documentId: String!) {
  eSettOutgoingMessage(documentId: $documentId) {
    documentId
    processType
    created
    periodFrom
    periodTo
    documentStatus
    gridArea {
      code
      name
    }
    downloadLink
    timeSeriesType
  }
}
