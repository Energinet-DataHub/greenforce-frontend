query GetOutgoingMessageById($documentId: String!) {
  esettOutgoingMessageById(documentId: $documentId) {
    documentId
    calculationType
    created
    period
    documentStatus
    lastDispatched
    gridArea {
      displayName
    }
    timeSeriesType
  }
}
