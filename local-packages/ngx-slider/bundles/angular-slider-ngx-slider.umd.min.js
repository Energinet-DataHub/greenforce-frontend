/**
 * @license
 * Copyright 2020 Energinet DataHub A/S
 *
 * Licensed under the Apache License, Version 2.0 (the "License2");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
!(function (e, i) {
  'object' == typeof exports && 'undefined' != typeof module
    ? i(
        exports,
        require('rxjs'),
        require('rxjs/operators'),
        require('detect-passive-events'),
        require('@angular/core'),
        require('@angular/forms'),
        require('@angular/common')
      )
    : 'function' == typeof define && define.amd
    ? define(
        '@angular-slider/ngx-slider',
        [
          'exports',
          'rxjs',
          'rxjs/operators',
          'detect-passive-events',
          '@angular/core',
          '@angular/forms',
          '@angular/common',
        ],
        i
      )
    : i(
        ((e['angular-slider'] = e['angular-slider'] || {}),
        (e['angular-slider']['ngx-slider'] = {})),
        e.rxjs,
        e.rxjs.operators,
        null,
        e.ng.core,
        e.ng.forms,
        e.ng.common
      );
})(this, function (e, l, a, r, s, i, t) {
  'use strict';
  var n =
    Object.setPrototypeOf ||
    ({ __proto__: [] } instanceof Array &&
      function (e, i) {
        e.__proto__ = i;
      }) ||
    function (e, i) {
      for (var t in i) i.hasOwnProperty(t) && (e[t] = i[t]);
    };
  function o(e, i) {
    function t() {
      this.constructor = e;
    }
    n(e, i),
      (e.prototype =
        null === i ? Object.create(i) : ((t.prototype = i.prototype), new t()));
  }
  function h(e) {
    var i = 'function' == typeof Symbol && e[Symbol.iterator],
      t = 0;
    return i
      ? i.call(e)
      : {
          next: function () {
            return (
              e && t >= e.length && (e = void 0),
              { value: e && e[t++], done: !e }
            );
          },
        };
  }
  var u = { Low: 0, High: 1, Floor: 2, Ceil: 3, TickValue: 4 };
  (u[u.Low] = 'Low'),
    (u[u.High] = 'High'),
    (u[u.Floor] = 'Floor'),
    (u[u.Ceil] = 'Ceil'),
    (u[u.TickValue] = 'TickValue');
  var p = function () {
      (this.floor = 0),
        (this.ceil = null),
        (this.step = 1),
        (this.minRange = null),
        (this.maxRange = null),
        (this.pushRange = !1),
        (this.minLimit = null),
        (this.maxLimit = null),
        (this.translate = null),
        (this.combineLabels = null),
        (this.getLegend = null),
        (this.getStepLegend = null),
        (this.stepsArray = null),
        (this.bindIndexForStepsArray = !1),
        (this.draggableRange = !1),
        (this.draggableRangeOnly = !1),
        (this.showSelectionBar = !1),
        (this.showSelectionBarEnd = !1),
        (this.showSelectionBarFromValue = null),
        (this.showOuterSelectionBars = !1),
        (this.hidePointerLabels = !1),
        (this.hideLimitLabels = !1),
        (this.autoHideLimitLabels = !0),
        (this.readOnly = !1),
        (this.disabled = !1),
        (this.showTicks = !1),
        (this.showTicksValues = !1),
        (this.tickStep = null),
        (this.tickValueStep = null),
        (this.ticksArray = null),
        (this.ticksTooltip = null),
        (this.ticksValuesTooltip = null),
        (this.vertical = !1),
        (this.getSelectionBarColor = null),
        (this.getTickColor = null),
        (this.getPointerColor = null),
        (this.keyboardSupport = !0),
        (this.scale = 1),
        (this.rotate = 0),
        (this.enforceStep = !0),
        (this.enforceRange = !0),
        (this.enforceStepsArray = !0),
        (this.noSwitching = !1),
        (this.onlyBindHandles = !1),
        (this.rightToLeft = !1),
        (this.reversedControls = !1),
        (this.boundPointerLabels = !0),
        (this.logScale = !1),
        (this.customValueToPosition = null),
        (this.customPositionToValue = null),
        (this.precisionLimit = 12),
        (this.selectionBarGradient = null),
        (this.ariaLabel = 'ngx-slider'),
        (this.ariaLabelledBy = null),
        (this.ariaLabelHigh = 'ngx-slider-max'),
        (this.ariaLabelledByHigh = null),
        (this.handleDimension = null),
        (this.barDimension = null),
        (this.animate = !0),
        (this.animateOnMove = !1);
    },
    d = { Min: 0, Max: 1 };
  (d[d.Min] = 'Min'), (d[d.Max] = 'Max');
  var c = function () {},
    g =
      ((v.isNullOrUndefined = function (e) {
        return e === undefined || null === e;
      }),
      (v.areArraysEqual = function (e, i) {
        if (e.length !== i.length) return !1;
        for (var t = 0; t < e.length; ++t) if (e[t] !== i[t]) return !1;
        return !0;
      }),
      (v.linearValueToPosition = function (e, i, t) {
        return (e - i) / (t - i);
      }),
      (v.logValueToPosition = function (e, i, t) {
        return (
          ((e = Math.log(e)) - (i = Math.log(i))) / ((t = Math.log(t)) - i)
        );
      }),
      (v.linearPositionToValue = function (e, i, t) {
        return e * (t - i) + i;
      }),
      (v.logPositionToValue = function (e, i, t) {
        i = Math.log(i);
        i = e * ((t = Math.log(t)) - i) + i;
        return Math.exp(i);
      }),
      (v.findStepIndex = function (i, e) {
        for (
          var t = e.map(function (e) {
              return Math.abs(i - e.value);
            }),
            n = 0,
            s = 0;
          s < e.length;
          s++
        )
          t[s] !== t[n] && t[s] < t[n] && (n = s);
        return n;
      }),
      v);
  function v() {}
  var m =
    ((f.isTouchEvent = function (e) {
      return window.TouchEvent !== undefined
        ? e instanceof TouchEvent
        : e.touches !== undefined;
    }),
    (f.isResizeObserverAvailable = function () {
      return window.ResizeObserver !== undefined;
    }),
    f);
  function f() {}
  var w =
    ((b.roundToPrecisionLimit = function (e, i) {
      return +e.toPrecision(i);
    }),
    (b.isModuloWithinPrecisionLimit = function (e, i, t) {
      t = Math.pow(10, -t);
      return Math.abs(e % i) <= t || Math.abs(Math.abs(e % i) - i) <= t;
    }),
    (b.clampToRange = function (e, i, t) {
      return Math.min(Math.max(e, i), t);
    }),
    b);
  function b() {}
  var O = function () {
      (this.eventName = null),
        (this.events = null),
        (this.eventsSubscription = null),
        (this.teardownCallback = null);
    },
    x =
      ((y.prototype.attachPassiveEventListener = function (e, i, t, n) {
        if (!0 !== r.supportsPassiveEvents)
          return this.attachEventListener(e, i, t, n);
        var s = new O();
        (s.eventName = i), (s.events = new l.Subject());
        var o = function (e) {
          s.events.next(e);
        };
        return (
          e.addEventListener(i, o, { passive: !0, capture: !1 }),
          (s.teardownCallback = function () {
            e.removeEventListener(i, o, { passive: !0, capture: !1 });
          }),
          (s.eventsSubscription = s.events
            .pipe(
              g.isNullOrUndefined(n)
                ? a.tap(function () {})
                : a.throttleTime(n, undefined, { leading: !0, trailing: !0 })
            )
            .subscribe(function (e) {
              t(e);
            })),
          s
        );
      }),
      (y.prototype.detachEventListener = function (e) {
        g.isNullOrUndefined(e.eventsSubscription) ||
          (e.eventsSubscription.unsubscribe(), (e.eventsSubscription = null)),
          g.isNullOrUndefined(e.events) ||
            (e.events.complete(), (e.events = null)),
          g.isNullOrUndefined(e.teardownCallback) ||
            (e.teardownCallback(), (e.teardownCallback = null));
      }),
      (y.prototype.attachEventListener = function (e, i, t, n) {
        var s = new O();
        (s.eventName = i), (s.events = new l.Subject());
        return (
          (s.teardownCallback = this.renderer.listen(e, i, function (e) {
            s.events.next(e);
          })),
          (s.eventsSubscription = s.events
            .pipe(
              g.isNullOrUndefined(n)
                ? a.tap(function () {})
                : a.throttleTime(n, undefined, { leading: !0, trailing: !0 })
            )
            .subscribe(function (e) {
              t(e);
            })),
          s
        );
      }),
      y);
  function y(e) {
    this.renderer = e;
  }
  var L =
    (Object.defineProperty(E.prototype, 'position', {
      get: function () {
        return this._position;
      },
      enumerable: !0,
      configurable: !0,
    }),
    Object.defineProperty(E.prototype, 'dimension', {
      get: function () {
        return this._dimension;
      },
      enumerable: !0,
      configurable: !0,
    }),
    Object.defineProperty(E.prototype, 'alwaysHide', {
      get: function () {
        return this._alwaysHide;
      },
      enumerable: !0,
      configurable: !0,
    }),
    Object.defineProperty(E.prototype, 'vertical', {
      get: function () {
        return this._vertical;
      },
      enumerable: !0,
      configurable: !0,
    }),
    Object.defineProperty(E.prototype, 'scale', {
      get: function () {
        return this._scale;
      },
      enumerable: !0,
      configurable: !0,
    }),
    Object.defineProperty(E.prototype, 'rotate', {
      get: function () {
        return this._rotate;
      },
      enumerable: !0,
      configurable: !0,
    }),
    (E.prototype.setAlwaysHide = function (e) {
      (this._alwaysHide = e), (this.visibility = e ? 'hidden' : 'visible');
    }),
    (E.prototype.hide = function () {
      this.opacity = 0;
    }),
    (E.prototype.show = function () {
      this.alwaysHide || (this.opacity = 1);
    }),
    (E.prototype.isVisible = function () {
      return !this.alwaysHide && 0 !== this.opacity;
    }),
    (E.prototype.setVertical = function (e) {
      (this._vertical = e),
        this._vertical
          ? ((this.left = ''), (this.width = ''))
          : ((this.bottom = ''), (this.height = ''));
    }),
    (E.prototype.setScale = function (e) {
      this._scale = e;
    }),
    (E.prototype.setRotate = function (e) {
      (this._rotate = e), (this.transform = 'rotate(' + e + 'deg)');
    }),
    (E.prototype.getRotate = function () {
      return this._rotate;
    }),
    (E.prototype.setPosition = function (e) {
      this._position === e ||
        this.isRefDestroyed() ||
        this.changeDetectionRef.markForCheck(),
        (this._position = e),
        this._vertical
          ? (this.bottom = Math.round(e) + 'px')
          : (this.left = Math.round(e) + 'px');
    }),
    (E.prototype.calculateDimension = function () {
      var e = this.getBoundingClientRect();
      this.vertical
        ? (this._dimension = (e.bottom - e.top) * this.scale)
        : (this._dimension = (e.right - e.left) * this.scale);
    }),
    (E.prototype.setDimension = function (e) {
      this._dimension === e ||
        this.isRefDestroyed() ||
        this.changeDetectionRef.markForCheck(),
        (this._dimension = e),
        this._vertical
          ? (this.height = Math.round(e) + 'px')
          : (this.width = Math.round(e) + 'px');
    }),
    (E.prototype.getBoundingClientRect = function () {
      return this.elemRef.nativeElement.getBoundingClientRect();
    }),
    (E.prototype.on = function (e, i, t) {
      t = this.eventListenerHelper.attachEventListener(
        this.elemRef.nativeElement,
        e,
        i,
        t
      );
      this.eventListeners.push(t);
    }),
    (E.prototype.onPassive = function (e, i, t) {
      t = this.eventListenerHelper.attachPassiveEventListener(
        this.elemRef.nativeElement,
        e,
        i,
        t
      );
      this.eventListeners.push(t);
    }),
    (E.prototype.off = function (i) {
      var e,
        t,
        n,
        s = g.isNullOrUndefined(i)
          ? ((e = []), this.eventListeners)
          : ((e = this.eventListeners.filter(function (e) {
              return e.eventName !== i;
            })),
            this.eventListeners.filter(function (e) {
              return e.eventName === i;
            }));
      try {
        for (var o = h(s), l = o.next(); !l.done; l = o.next()) {
          var a = l.value;
          this.eventListenerHelper.detachEventListener(a);
        }
      } catch (r) {
        t = { error: r };
      } finally {
        try {
          l && !l.done && (n = o['return']) && n.call(o);
        } finally {
          if (t) throw t.error;
        }
      }
      this.eventListeners = e;
    }),
    (E.prototype.isRefDestroyed = function () {
      return (
        g.isNullOrUndefined(this.changeDetectionRef) ||
        this.changeDetectionRef.destroyed
      );
    }),
    (E.decorators = [
      { type: s.Directive, args: [{ selector: '[ngxSliderElement]' }] },
    ]),
    (E.ctorParameters = function () {
      return [
        { type: s.ElementRef },
        { type: s.Renderer2 },
        { type: s.ChangeDetectorRef },
      ];
    }),
    (E.propDecorators = {
      opacity: [{ type: s.HostBinding, args: ['style.opacity'] }],
      visibility: [{ type: s.HostBinding, args: ['style.visibility'] }],
      left: [{ type: s.HostBinding, args: ['style.left'] }],
      bottom: [{ type: s.HostBinding, args: ['style.bottom'] }],
      height: [{ type: s.HostBinding, args: ['style.height'] }],
      width: [{ type: s.HostBinding, args: ['style.width'] }],
      transform: [{ type: s.HostBinding, args: ['style.transform'] }],
    }),
    E);
  function E(e, i, t) {
    (this.elemRef = e),
      (this.renderer = i),
      (this.changeDetectionRef = t),
      (this._position = 0),
      (this._dimension = 0),
      (this._alwaysHide = !1),
      (this._vertical = !1),
      (this._scale = 1),
      (this._rotate = 0),
      (this.opacity = 1),
      (this.visibility = 'visible'),
      (this.left = ''),
      (this.bottom = ''),
      (this.height = ''),
      (this.width = ''),
      (this.transform = ''),
      (this.eventListeners = []),
      (this.eventListenerHelper = new x(this.renderer));
  }
  var V,
    H =
      (o(S, (V = L)),
      (S.prototype.focus = function () {
        this.elemRef.nativeElement.focus();
      }),
      (S.decorators = [
        { type: s.Directive, args: [{ selector: '[ngxSliderHandle]' }] },
      ]),
      (S.ctorParameters = function () {
        return [
          { type: s.ElementRef },
          { type: s.Renderer2 },
          { type: s.ChangeDetectorRef },
        ];
      }),
      (S.propDecorators = {
        active: [{ type: s.HostBinding, args: ['class.ngx-slider-active'] }],
        role: [{ type: s.HostBinding, args: ['attr.role'] }],
        tabindex: [{ type: s.HostBinding, args: ['attr.tabindex'] }],
        ariaOrientation: [
          { type: s.HostBinding, args: ['attr.aria-orientation'] },
        ],
        ariaLabel: [{ type: s.HostBinding, args: ['attr.aria-label'] }],
        ariaLabelledBy: [
          { type: s.HostBinding, args: ['attr.aria-labelledby'] },
        ],
        ariaValueNow: [{ type: s.HostBinding, args: ['attr.aria-valuenow'] }],
        ariaValueText: [{ type: s.HostBinding, args: ['attr.aria-valuetext'] }],
        ariaValueMin: [{ type: s.HostBinding, args: ['attr.aria-valuemin'] }],
        ariaValueMax: [{ type: s.HostBinding, args: ['attr.aria-valuemax'] }],
      }),
      S);
  function S(e, i, t) {
    t = V.call(this, e, i, t) || this;
    return (
      (t.active = !1),
      (t.role = ''),
      (t.tabindex = ''),
      (t.ariaOrientation = ''),
      (t.ariaLabel = ''),
      (t.ariaLabelledBy = ''),
      (t.ariaValueNow = ''),
      (t.ariaValueText = ''),
      (t.ariaValueMin = ''),
      (t.ariaValueMax = ''),
      t
    );
  }
  var T,
    C =
      (o(P, (T = L)),
      Object.defineProperty(P.prototype, 'value', {
        get: function () {
          return this._value;
        },
        enumerable: !0,
        configurable: !0,
      }),
      (P.prototype.setValue = function (e) {
        var i = !1;
        !this.alwaysHide &&
          (g.isNullOrUndefined(this.value) ||
            this.value.length !== e.length ||
            (0 < this.value.length && 0 === this.dimension)) &&
          (i = !0),
          (this._value = e),
          (this.elemRef.nativeElement.innerHTML = e),
          i && this.calculateDimension();
      }),
      (P.decorators = [
        { type: s.Directive, args: [{ selector: '[ngxSliderLabel]' }] },
      ]),
      (P.ctorParameters = function () {
        return [
          { type: s.ElementRef },
          { type: s.Renderer2 },
          { type: s.ChangeDetectorRef },
        ];
      }),
      P);
  function P(e, i, t) {
    t = T.call(this, e, i, t) || this;
    return (t._value = null), t;
  }
  var M = function () {
      (this.selected = !1),
        (this.style = {}),
        (this.tooltip = null),
        (this.tooltipPlacement = null),
        (this.value = null),
        (this.valueTooltip = null),
        (this.valueTooltipPlacement = null),
        (this.legend = null);
    },
    k = function () {
      (this.active = !1),
        (this.value = 0),
        (this.difference = 0),
        (this.position = 0),
        (this.lowLimit = 0),
        (this.highLimit = 0);
    },
    B =
      ((N.compare = function (e, i) {
        return (
          (!g.isNullOrUndefined(e) || !g.isNullOrUndefined(i)) &&
          g.isNullOrUndefined(e) === g.isNullOrUndefined(i) &&
          e.value === i.value &&
          e.highValue === i.highValue
        );
      }),
      N);
  function N() {}
  var R,
    D =
      (o(U, (R = B)),
      (U.compare = function (e, i) {
        return (
          (!g.isNullOrUndefined(e) || !g.isNullOrUndefined(i)) &&
          g.isNullOrUndefined(e) === g.isNullOrUndefined(i) &&
          e.value === i.value &&
          e.highValue === i.highValue &&
          e.forceChange === i.forceChange
        );
      }),
      U);
  function U() {
    return (null !== R && R.apply(this, arguments)) || this;
  }
  var A;
  o(F, (A = D));
  function F() {
    return (null !== A && A.apply(this, arguments)) || this;
  }
  var I;
  o(j, (I = D));
  function j() {
    return (null !== I && I.apply(this, arguments)) || this;
  }
  var i = {
      provide: i.NG_VALUE_ACCESSOR,
      useExisting: s.forwardRef(function () {
        return _;
      }),
      multi: !0,
    },
    _ =
      (Object.defineProperty(z.prototype, 'manualRefresh', {
        set: function (e) {
          var i = this;
          this.unsubscribeManualRefresh(),
            (this.manualRefreshSubscription = e.subscribe(function () {
              setTimeout(function () {
                return i.calculateViewDimensionsAndDetectChanges();
              });
            }));
        },
        enumerable: !0,
        configurable: !0,
      }),
      Object.defineProperty(z.prototype, 'triggerFocus', {
        set: function (e) {
          var i = this;
          this.unsubscribeTriggerFocus(),
            (this.triggerFocusSubscription = e.subscribe(function (e) {
              i.focusPointer(e);
            }));
        },
        enumerable: !0,
        configurable: !0,
      }),
      Object.defineProperty(z.prototype, 'range', {
        get: function () {
          return (
            !g.isNullOrUndefined(this.value) &&
            !g.isNullOrUndefined(this.highValue)
          );
        },
        enumerable: !0,
        configurable: !0,
      }),
      Object.defineProperty(z.prototype, 'showTicks', {
        get: function () {
          return this.viewOptions.showTicks;
        },
        enumerable: !0,
        configurable: !0,
      }),
      (z.prototype.ngOnInit = function () {
        (this.viewOptions = new p()),
          Object.assign(this.viewOptions, this.options),
          this.updateDisabledState(),
          this.updateVerticalState(),
          this.updateAriaLabel();
      }),
      (z.prototype.ngAfterViewInit = function () {
        console.log('ngAfterViewInit angular-slider-ngx-slider.umd.min.js');
        this.applyOptions(),
          this.subscribeInputModelChangeSubject(),
          this.subscribeOutputModelChangeSubject(),
          this.renormaliseModelValues(),
          (this.viewLowValue = this.modelValueToViewValue(this.value)),
          this.range
            ? (this.viewHighValue = this.modelValueToViewValue(this.highValue))
            : (this.viewHighValue = null),
          this.updateVerticalState(),
          this.manageElementsStyle(),
          this.updateDisabledState(),
          this.calculateViewDimensions(),
          this.addAccessibility(),
          this.updateCeilLabel(),
          this.updateFloorLabel(),
          this.initHandles(),
          this.manageEventsBindings(),
          this.updateAriaLabel(),
          this.subscribeResizeObserver(),
          (this.initHasRun = !0),
          this.isRefDestroyed() || this.changeDetectionRef.detectChanges();
      }),
      (z.prototype.ngOnChanges = function (e) {
        g.isNullOrUndefined(e.options) ||
          JSON.stringify(e.options.previousValue) ===
            JSON.stringify(e.options.currentValue) ||
          this.onChangeOptions(),
          (g.isNullOrUndefined(e.value) && g.isNullOrUndefined(e.highValue)) ||
            this.inputModelChangeSubject.next({
              value: this.value,
              highValue: this.highValue,
              forceChange: !1,
              internalChange: !1,
            });
      }),
      (z.prototype.ngOnDestroy = function () {
        this.unbindEvents(),
          this.unsubscribeResizeObserver(),
          this.unsubscribeInputModelChangeSubject(),
          this.unsubscribeOutputModelChangeSubject(),
          this.unsubscribeManualRefresh(),
          this.unsubscribeTriggerFocus();
      }),
      (z.prototype.writeValue = function (e) {
        e instanceof Array
          ? ((this.value = e[0]), (this.highValue = e[1]))
          : (this.value = e),
          this.inputModelChangeSubject.next({
            value: this.value,
            highValue: this.highValue,
            forceChange: !1,
            internalChange: !1,
          });
      }),
      (z.prototype.registerOnChange = function (e) {
        this.onChangeCallback = e;
      }),
      (z.prototype.registerOnTouched = function (e) {
        this.onTouchedCallback = e;
      }),
      (z.prototype.setDisabledState = function (e) {
        (this.viewOptions.disabled = e), this.updateDisabledState();
      }),
      (z.prototype.setAriaLabel = function (e) {
        (this.viewOptions.ariaLabel = e), this.updateAriaLabel();
      }),
      (z.prototype.onResize = function (e) {
        this.calculateViewDimensionsAndDetectChanges();
      }),
      (z.prototype.subscribeInputModelChangeSubject = function () {
        var i = this;
        this.inputModelChangeSubscription = this.inputModelChangeSubject
          .pipe(
            a.distinctUntilChanged(D.compare),
            a.filter(function (e) {
              return !e.forceChange && !e.internalChange;
            })
          )
          .subscribe(function (e) {
            return i.applyInputModelChange(e);
          });
      }),
      (z.prototype.subscribeOutputModelChangeSubject = function () {
        var i = this;
        this.outputModelChangeSubscription = this.outputModelChangeSubject
          .pipe(a.distinctUntilChanged(D.compare))
          .subscribe(function (e) {
            return i.publishOutputModelChange(e);
          });
      }),
      (z.prototype.subscribeResizeObserver = function () {
        var e = this;
        m.isResizeObserverAvailable() &&
          ((this.resizeObserver = new ResizeObserver(function () {
            return e.calculateViewDimensionsAndDetectChanges();
          })),
          this.resizeObserver.observe(this.elementRef.nativeElement));
      }),
      (z.prototype.unsubscribeResizeObserver = function () {
        m.isResizeObserverAvailable() &&
          null !== this.resizeObserver &&
          (this.resizeObserver.disconnect(), (this.resizeObserver = null));
      }),
      (z.prototype.unsubscribeOnMove = function () {
        g.isNullOrUndefined(this.onMoveEventListener) ||
          (this.eventListenerHelper.detachEventListener(
            this.onMoveEventListener
          ),
          (this.onMoveEventListener = null));
      }),
      (z.prototype.unsubscribeOnEnd = function () {
        g.isNullOrUndefined(this.onEndEventListener) ||
          (this.eventListenerHelper.detachEventListener(
            this.onEndEventListener
          ),
          (this.onEndEventListener = null));
      }),
      (z.prototype.unsubscribeInputModelChangeSubject = function () {
        g.isNullOrUndefined(this.inputModelChangeSubscription) ||
          (this.inputModelChangeSubscription.unsubscribe(),
          (this.inputModelChangeSubscription = null));
      }),
      (z.prototype.unsubscribeOutputModelChangeSubject = function () {
        g.isNullOrUndefined(this.outputModelChangeSubscription) ||
          (this.outputModelChangeSubscription.unsubscribe(),
          (this.outputModelChangeSubscription = null));
      }),
      (z.prototype.unsubscribeManualRefresh = function () {
        g.isNullOrUndefined(this.manualRefreshSubscription) ||
          (this.manualRefreshSubscription.unsubscribe(),
          (this.manualRefreshSubscription = null));
      }),
      (z.prototype.unsubscribeTriggerFocus = function () {
        g.isNullOrUndefined(this.triggerFocusSubscription) ||
          (this.triggerFocusSubscription.unsubscribe(),
          (this.triggerFocusSubscription = null));
      }),
      (z.prototype.getPointerElement = function (e) {
        return e === d.Min
          ? this.minHandleElement
          : e === d.Max
          ? this.maxHandleElement
          : null;
      }),
      (z.prototype.getCurrentTrackingValue = function () {
        return this.currentTrackingPointer === d.Min
          ? this.viewLowValue
          : this.currentTrackingPointer === d.Max
          ? this.viewHighValue
          : null;
      }),
      (z.prototype.modelValueToViewValue = function (e) {
        return g.isNullOrUndefined(e)
          ? NaN
          : g.isNullOrUndefined(this.viewOptions.stepsArray) ||
            this.viewOptions.bindIndexForStepsArray
          ? +e
          : g.findStepIndex(+e, this.viewOptions.stepsArray);
      }),
      (z.prototype.viewValueToModelValue = function (e) {
        return g.isNullOrUndefined(this.viewOptions.stepsArray) ||
          this.viewOptions.bindIndexForStepsArray
          ? e
          : this.getStepValue(e);
      }),
      (z.prototype.getStepValue = function (e) {
        e = this.viewOptions.stepsArray[e];
        return g.isNullOrUndefined(e) ? NaN : e.value;
      }),
      (z.prototype.applyViewChange = function () {
        (this.value = this.viewValueToModelValue(this.viewLowValue)),
          this.range &&
            (this.highValue = this.viewValueToModelValue(this.viewHighValue)),
          this.outputModelChangeSubject.next({
            value: this.value,
            highValue: this.highValue,
            userEventInitiated: !0,
            forceChange: !1,
          }),
          this.inputModelChangeSubject.next({
            value: this.value,
            highValue: this.highValue,
            forceChange: !1,
            internalChange: !0,
          });
      }),
      (z.prototype.applyInputModelChange = function (e) {
        var i = this.normaliseModelValues(e),
          e = !B.compare(e, i);
        e && ((this.value = i.value), (this.highValue = i.highValue)),
          (this.viewLowValue = this.modelValueToViewValue(i.value)),
          this.range
            ? (this.viewHighValue = this.modelValueToViewValue(i.highValue))
            : (this.viewHighValue = null),
          this.updateLowHandle(this.valueToPosition(this.viewLowValue)),
          this.range &&
            this.updateHighHandle(this.valueToPosition(this.viewHighValue)),
          this.updateSelectionBar(),
          this.updateTicksScale(),
          this.updateAriaAttributes(),
          this.range && this.updateCombinedLabel(),
          this.outputModelChangeSubject.next({
            value: i.value,
            highValue: i.highValue,
            forceChange: e,
            userEventInitiated: !1,
          });
      }),
      (z.prototype.publishOutputModelChange = function (e) {
        var i = this,
          t = function () {
            i.valueChange.emit(e.value),
              i.range && i.highValueChange.emit(e.highValue),
              g.isNullOrUndefined(i.onChangeCallback) ||
                (i.range
                  ? i.onChangeCallback([e.value, e.highValue])
                  : i.onChangeCallback(e.value)),
              g.isNullOrUndefined(i.onTouchedCallback) ||
                (i.range
                  ? i.onTouchedCallback([e.value, e.highValue])
                  : i.onTouchedCallback(e.value));
          };
        e.userEventInitiated
          ? (t(), this.userChange.emit(this.getChangeContext()))
          : setTimeout(function () {
              t();
            });
      }),
      (z.prototype.normaliseModelValues = function (e) {
        var i,
          t = new B();
        return (
          (t.value = e.value),
          (t.highValue = e.highValue),
          g.isNullOrUndefined(this.viewOptions.stepsArray)
            ? (this.viewOptions.enforceStep &&
                ((t.value = this.roundStep(t.value)),
                this.range && (t.highValue = this.roundStep(t.highValue))),
              this.viewOptions.enforceRange &&
                ((t.value = w.clampToRange(
                  t.value,
                  this.viewOptions.floor,
                  this.viewOptions.ceil
                )),
                this.range &&
                  (t.highValue = w.clampToRange(
                    t.highValue,
                    this.viewOptions.floor,
                    this.viewOptions.ceil
                  )),
                this.range &&
                  e.value > e.highValue &&
                  (this.viewOptions.noSwitching
                    ? (t.value = t.highValue)
                    : ((i = e.value),
                      (t.value = e.highValue),
                      (t.highValue = i)))))
            : this.viewOptions.enforceStepsArray &&
              ((i = g.findStepIndex(t.value, this.viewOptions.stepsArray)),
              (t.value = this.viewOptions.stepsArray[i].value),
              this.range &&
                ((i = g.findStepIndex(
                  t.highValue,
                  this.viewOptions.stepsArray
                )),
                (t.highValue = this.viewOptions.stepsArray[i].value))),
          t
        );
      }),
      (z.prototype.renormaliseModelValues = function () {
        var e = { value: this.value, highValue: this.highValue },
          i = this.normaliseModelValues(e);
        B.compare(i, e) ||
          ((this.value = i.value),
          (this.highValue = i.highValue),
          this.outputModelChangeSubject.next({
            value: this.value,
            highValue: this.highValue,
            forceChange: !0,
            userEventInitiated: !1,
          }));
      }),
      (z.prototype.onChangeOptions = function () {
        var e, i;
        this.initHasRun &&
          ((e = this.getOptionsInfluencingEventBindings(this.viewOptions)),
          this.applyOptions(),
          (i = this.getOptionsInfluencingEventBindings(this.viewOptions)),
          (i = !g.areArraysEqual(e, i)),
          this.renormaliseModelValues(),
          (this.viewLowValue = this.modelValueToViewValue(this.value)),
          this.range
            ? (this.viewHighValue = this.modelValueToViewValue(this.highValue))
            : (this.viewHighValue = null),
          this.resetSlider(i));
      }),
      (z.prototype.applyOptions = function () {
        if (
          ((this.viewOptions = new p()),
          Object.assign(this.viewOptions, this.options),
          (this.viewOptions.draggableRange =
            this.range && this.viewOptions.draggableRange),
          (this.viewOptions.draggableRangeOnly =
            this.range && this.viewOptions.draggableRangeOnly),
          this.viewOptions.draggableRangeOnly &&
            (this.viewOptions.draggableRange = !0),
          (this.viewOptions.showTicks =
            this.viewOptions.showTicks ||
            this.viewOptions.showTicksValues ||
            !g.isNullOrUndefined(this.viewOptions.ticksArray)),
          !this.viewOptions.showTicks ||
            (g.isNullOrUndefined(this.viewOptions.tickStep) &&
              g.isNullOrUndefined(this.viewOptions.ticksArray)) ||
            (this.intermediateTicks = !0),
          (this.viewOptions.showSelectionBar =
            this.viewOptions.showSelectionBar ||
            this.viewOptions.showSelectionBarEnd ||
            !g.isNullOrUndefined(this.viewOptions.showSelectionBarFromValue)),
          g.isNullOrUndefined(this.viewOptions.stepsArray)
            ? this.applyFloorCeilOptions()
            : this.applyStepsArrayOptions(),
          g.isNullOrUndefined(this.viewOptions.combineLabels) &&
            (this.viewOptions.combineLabels = function (e, i) {
              return e + ' - ' + i;
            }),
          this.viewOptions.logScale && 0 === this.viewOptions.floor)
        )
          throw Error("Can't use floor=0 with logarithmic scale");
      }),
      (z.prototype.applyStepsArrayOptions = function () {
        var i = this;
        (this.viewOptions.floor = 0),
          (this.viewOptions.ceil = this.viewOptions.stepsArray.length - 1),
          (this.viewOptions.step = 1),
          g.isNullOrUndefined(this.viewOptions.translate) &&
            (this.viewOptions.translate = function (e) {
              return i.viewOptions.bindIndexForStepsArray
                ? String(i.getStepValue(e))
                : String(e);
            });
      }),
      (z.prototype.applyFloorCeilOptions = function () {
        if (
          (g.isNullOrUndefined(this.viewOptions.step)
            ? (this.viewOptions.step = 1)
            : ((this.viewOptions.step = +this.viewOptions.step),
              this.viewOptions.step <= 0 && (this.viewOptions.step = 1)),
          g.isNullOrUndefined(this.viewOptions.ceil) ||
            g.isNullOrUndefined(this.viewOptions.floor))
        )
          throw Error('floor and ceil options must be supplied');
        (this.viewOptions.ceil = +this.viewOptions.ceil),
          (this.viewOptions.floor = +this.viewOptions.floor),
          g.isNullOrUndefined(this.viewOptions.translate) &&
            (this.viewOptions.translate = function (e) {
              return String(e);
            });
      }),
      (z.prototype.resetSlider = function (e) {
        void 0 === e && (e = !0),
          this.manageElementsStyle(),
          this.addAccessibility(),
          this.updateCeilLabel(),
          this.updateFloorLabel(),
          e && (this.unbindEvents(), this.manageEventsBindings()),
          this.updateDisabledState(),
          this.updateAriaLabel(),
          this.calculateViewDimensions(),
          this.refocusPointerIfNeeded();
      }),
      (z.prototype.focusPointer = function (e) {
        e !== d.Min && e !== d.Max && (e = d.Min),
          e === d.Min
            ? this.minHandleElement.focus()
            : this.range && e === d.Max && this.maxHandleElement.focus();
      }),
      (z.prototype.refocusPointerIfNeeded = function () {
        g.isNullOrUndefined(this.currentFocusPointer) ||
          (this.onPointerFocus(this.currentFocusPointer),
          this.getPointerElement(this.currentFocusPointer).focus());
      }),
      (z.prototype.manageElementsStyle = function () {
        var e = this;
        this.updateScale(),
          this.floorLabelElement.setAlwaysHide(
            this.viewOptions.showTicksValues || this.viewOptions.hideLimitLabels
          ),
          this.ceilLabelElement.setAlwaysHide(
            this.viewOptions.showTicksValues || this.viewOptions.hideLimitLabels
          );
        var i = this.viewOptions.showTicksValues && !this.intermediateTicks;
        this.minHandleLabelElement.setAlwaysHide(
          i || this.viewOptions.hidePointerLabels
        ),
          this.maxHandleLabelElement.setAlwaysHide(
            i || !this.range || this.viewOptions.hidePointerLabels
          ),
          this.combinedLabelElement.setAlwaysHide(
            i || !this.range || this.viewOptions.hidePointerLabels
          ),
          this.selectionBarElement.setAlwaysHide(
            !this.range && !this.viewOptions.showSelectionBar
          ),
          this.leftOuterSelectionBarElement.setAlwaysHide(
            !this.range || !this.viewOptions.showOuterSelectionBars
          ),
          this.rightOuterSelectionBarElement.setAlwaysHide(
            !this.range || !this.viewOptions.showOuterSelectionBars
          ),
          (this.fullBarTransparentClass =
            this.range && this.viewOptions.showOuterSelectionBars),
          (this.selectionBarDraggableClass =
            this.viewOptions.draggableRange &&
            !this.viewOptions.onlyBindHandles),
          (this.ticksUnderValuesClass =
            this.intermediateTicks && this.options.showTicksValues),
          this.sliderElementVerticalClass !== this.viewOptions.vertical &&
            (this.updateVerticalState(),
            setTimeout(function () {
              e.resetSlider();
            })),
          this.sliderElementAnimateClass !== this.viewOptions.animate &&
            setTimeout(function () {
              e.sliderElementAnimateClass = e.viewOptions.animate;
            }),
          this.updateRotate();
      }),
      (z.prototype.manageEventsBindings = function () {
        this.viewOptions.disabled || this.viewOptions.readOnly
          ? this.unbindEvents()
          : this.bindEvents();
      }),
      (z.prototype.updateDisabledState = function () {
        this.sliderElementDisabledAttr = this.viewOptions.disabled
          ? 'disabled'
          : null;
      }),
      (z.prototype.updateAriaLabel = function () {
        this.sliderElementAriaLabel =
          this.viewOptions.ariaLabel || 'nxg-slider';
      }),
      (z.prototype.updateVerticalState = function () {
        this.sliderElementVerticalClass = this.viewOptions.vertical;
        try {
          for (
            var e = h(this.getAllSliderElements()), i = e.next();
            !i.done;
            i = e.next()
          ) {
            var t = i.value;
            g.isNullOrUndefined(t) || t.setVertical(this.viewOptions.vertical);
          }
        } catch (o) {
          n = { error: o };
        } finally {
          try {
            i && !i.done && (s = e['return']) && s.call(e);
          } finally {
            if (n) throw n.error;
          }
        }
        var n, s;
      }),
      (z.prototype.updateScale = function () {
        try {
          for (
            var e = h(this.getAllSliderElements()), i = e.next();
            !i.done;
            i = e.next()
          )
            i.value.setScale(this.viewOptions.scale);
        } catch (s) {
          t = { error: s };
        } finally {
          try {
            i && !i.done && (n = e['return']) && n.call(e);
          } finally {
            if (t) throw t.error;
          }
        }
        var t, n;
      }),
      (z.prototype.updateRotate = function () {
        try {
          for (
            var e = h(this.getAllSliderElements()), i = e.next();
            !i.done;
            i = e.next()
          )
            i.value.setRotate(this.viewOptions.rotate);
        } catch (s) {
          t = { error: s };
        } finally {
          try {
            i && !i.done && (n = e['return']) && n.call(e);
          } finally {
            if (t) throw t.error;
          }
        }
        var t, n;
      }),
      (z.prototype.getAllSliderElements = function () {
        return [
          this.leftOuterSelectionBarElement,
          this.rightOuterSelectionBarElement,
          this.fullBarElement,
          this.selectionBarElement,
          this.minHandleElement,
          this.maxHandleElement,
          this.floorLabelElement,
          this.ceilLabelElement,
          this.minHandleLabelElement,
          this.maxHandleLabelElement,
          this.combinedLabelElement,
          this.ticksElement,
        ];
      }),
      (z.prototype.initHandles = function () {
        this.updateLowHandle(this.valueToPosition(this.viewLowValue)),
          this.range &&
            this.updateHighHandle(this.valueToPosition(this.viewHighValue)),
          this.updateSelectionBar(),
          this.range && this.updateCombinedLabel(),
          this.updateTicksScale();
      }),
      (z.prototype.addAccessibility = function () {
        this.updateAriaAttributes(),
          (this.minHandleElement.role = 'slider'),
          !this.viewOptions.keyboardSupport ||
          this.viewOptions.readOnly ||
          this.viewOptions.disabled
            ? (this.minHandleElement.tabindex = '')
            : (this.minHandleElement.tabindex = '0'),
          (this.minHandleElement.ariaOrientation =
            this.viewOptions.vertical || 0 !== this.viewOptions.rotate
              ? 'vertical'
              : 'horizontal'),
          g.isNullOrUndefined(this.viewOptions.ariaLabel)
            ? g.isNullOrUndefined(this.viewOptions.ariaLabelledBy) ||
              (this.minHandleElement.ariaLabelledBy =
                this.viewOptions.ariaLabelledBy)
            : (this.minHandleElement.ariaLabel = this.viewOptions.ariaLabel),
          this.range &&
            ((this.maxHandleElement.role = 'slider'),
            !this.viewOptions.keyboardSupport ||
            this.viewOptions.readOnly ||
            this.viewOptions.disabled
              ? (this.maxHandleElement.tabindex = '')
              : (this.maxHandleElement.tabindex = '0'),
            (this.maxHandleElement.ariaOrientation =
              this.viewOptions.vertical || 0 !== this.viewOptions.rotate
                ? 'vertical'
                : 'horizontal'),
            g.isNullOrUndefined(this.viewOptions.ariaLabelHigh)
              ? g.isNullOrUndefined(this.viewOptions.ariaLabelledByHigh) ||
                (this.maxHandleElement.ariaLabelledBy =
                  this.viewOptions.ariaLabelledByHigh)
              : (this.maxHandleElement.ariaLabel =
                  this.viewOptions.ariaLabelHigh));
      }),
      (z.prototype.updateAriaAttributes = function () {
        (this.minHandleElement.ariaValueNow = (+this.value).toString()),
          (this.minHandleElement.ariaValueText = this.viewOptions.translate(
            +this.value,
            u.Low
          )),
          (this.minHandleElement.ariaValueMin =
            this.viewOptions.floor.toString()),
          (this.minHandleElement.ariaValueMax =
            this.viewOptions.ceil.toString()),
          this.range &&
            ((this.maxHandleElement.ariaValueNow = (+this
              .highValue).toString()),
            (this.maxHandleElement.ariaValueText = this.viewOptions.translate(
              +this.highValue,
              u.High
            )),
            (this.maxHandleElement.ariaValueMin =
              this.viewOptions.floor.toString()),
            (this.maxHandleElement.ariaValueMax =
              this.viewOptions.ceil.toString()));
      }),
      (z.prototype.calculateViewDimensions = function () {
        g.isNullOrUndefined(this.viewOptions.handleDimension)
          ? this.minHandleElement.calculateDimension()
          : this.minHandleElement.setDimension(
              this.viewOptions.handleDimension
            );
        var e = this.minHandleElement.dimension;
        (this.handleHalfDimension = e / 2),
          g.isNullOrUndefined(this.viewOptions.barDimension)
            ? this.fullBarElement.calculateDimension()
            : this.fullBarElement.setDimension(this.viewOptions.barDimension),
          (this.maxHandlePosition = this.fullBarElement.dimension - e),
          this.initHasRun &&
            (this.updateFloorLabel(),
            this.updateCeilLabel(),
            this.initHandles());
      }),
      (z.prototype.calculateViewDimensionsAndDetectChanges = function () {
        this.calculateViewDimensions(),
          this.isRefDestroyed() || this.changeDetectionRef.detectChanges();
      }),
      (z.prototype.isRefDestroyed = function () {
        return this.changeDetectionRef.destroyed;
      }),
      (z.prototype.updateTicksScale = function () {
        var n = this;
        if (this.viewOptions.showTicks) {
          var e = g.isNullOrUndefined(this.viewOptions.ticksArray)
              ? this.getTicksArray()
              : this.viewOptions.ticksArray,
            s = this.viewOptions.vertical ? 'translateY' : 'translateX';
          this.viewOptions.rightToLeft && e.reverse();
          var o = g.isNullOrUndefined(this.viewOptions.tickValueStep)
              ? g.isNullOrUndefined(this.viewOptions.tickStep)
                ? this.viewOptions.step
                : this.viewOptions.tickStep
              : this.viewOptions.tickValueStep,
            l = !1,
            i = e.map(function (e) {
              var i = n.valueToPosition(e);
              n.viewOptions.vertical && (i = n.maxHandlePosition - i);
              var t = s + '(' + Math.round(i) + 'px)',
                i = new M();
              (i.selected = n.isTickSelected(e)),
                (i.style = {
                  '-webkit-transform': t,
                  '-moz-transform': t,
                  '-o-transform': t,
                  '-ms-transform': t,
                  transform: t,
                }),
                i.selected &&
                  !g.isNullOrUndefined(n.viewOptions.getSelectionBarColor) &&
                  (i.style['background-color'] = n.getSelectionBarColor()),
                i.selected ||
                  g.isNullOrUndefined(n.viewOptions.getTickColor) ||
                  (i.style['background-color'] = n.getTickColor(e)),
                g.isNullOrUndefined(n.viewOptions.ticksTooltip) ||
                  ((i.tooltip = n.viewOptions.ticksTooltip(e)),
                  (i.tooltipPlacement = n.viewOptions.vertical
                    ? 'right'
                    : 'top')),
                n.viewOptions.showTicksValues &&
                  !g.isNullOrUndefined(o) &&
                  w.isModuloWithinPrecisionLimit(
                    e,
                    o,
                    n.viewOptions.precisionLimit
                  ) &&
                  ((i.value = n.getDisplayValue(e, u.TickValue)),
                  g.isNullOrUndefined(n.viewOptions.ticksValuesTooltip) ||
                    ((i.valueTooltip = n.viewOptions.ticksValuesTooltip(e)),
                    (i.valueTooltipPlacement = n.viewOptions.vertical
                      ? 'right'
                      : 'top')));
              t = null;
              return (
                g.isNullOrUndefined(n.viewOptions.stepsArray)
                  ? g.isNullOrUndefined(n.viewOptions.getLegend) ||
                    (t = n.viewOptions.getLegend(e))
                  : ((e = n.viewOptions.stepsArray[e]),
                    g.isNullOrUndefined(n.viewOptions.getStepLegend)
                      ? g.isNullOrUndefined(e) || (t = e.legend)
                      : (t = n.viewOptions.getStepLegend(e))),
                g.isNullOrUndefined(t) || ((i.legend = t), (l = !0)),
                i
              );
            });
          if (
            (setTimeout(function () {
              n.sliderElementWithLegendClass = l;
            }),
            g.isNullOrUndefined(this.ticks) || this.ticks.length !== i.length)
          )
            this.ticks = i;
          else
            for (var t = 0; t < i.length; ++t)
              Object.assign(this.ticks[t], i[t]);
          this.isRefDestroyed() || this.changeDetectionRef.detectChanges();
        } else
          setTimeout(function () {
            n.sliderElementWithLegendClass = !1;
          });
      }),
      (z.prototype.getTicksArray = function () {
        for (
          var e = g.isNullOrUndefined(this.viewOptions.tickStep)
              ? this.viewOptions.step
              : this.viewOptions.tickStep,
            i = [],
            t =
              1 +
              Math.floor(
                w.roundToPrecisionLimit(
                  Math.abs(this.viewOptions.ceil - this.viewOptions.floor) / e,
                  this.viewOptions.precisionLimit
                )
              ),
            n = 0;
          n < t;
          ++n
        )
          i.push(
            w.roundToPrecisionLimit(
              this.viewOptions.floor + e * n,
              this.viewOptions.precisionLimit
            )
          );
        return i;
      }),
      (z.prototype.isTickSelected = function (e) {
        if (!this.range)
          if (g.isNullOrUndefined(this.viewOptions.showSelectionBarFromValue)) {
            if (this.viewOptions.showSelectionBarEnd) {
              if (e >= this.viewLowValue) return !0;
            } else if (
              this.viewOptions.showSelectionBar &&
              e <= this.viewLowValue
            )
              return !0;
          } else {
            var i = this.viewOptions.showSelectionBarFromValue;
            if (this.viewLowValue > i && i <= e && e <= this.viewLowValue)
              return !0;
            if (this.viewLowValue < i && e <= i && e >= this.viewLowValue)
              return !0;
          }
        return !!(
          this.range &&
          e >= this.viewLowValue &&
          e <= this.viewHighValue
        );
      }),
      (z.prototype.updateFloorLabel = function () {
        var e;
        this.floorLabelElement.alwaysHide ||
          (this.floorLabelElement.setValue(
            this.getDisplayValue(this.viewOptions.floor, u.Floor)
          ),
          this.floorLabelElement.calculateDimension(),
          (e = this.viewOptions.rightToLeft
            ? this.fullBarElement.dimension - this.floorLabelElement.dimension
            : 0),
          this.floorLabelElement.setPosition(e));
      }),
      (z.prototype.updateCeilLabel = function () {
        var e;
        this.ceilLabelElement.alwaysHide ||
          (this.ceilLabelElement.setValue(
            this.getDisplayValue(this.viewOptions.ceil, u.Ceil)
          ),
          this.ceilLabelElement.calculateDimension(),
          (e = this.viewOptions.rightToLeft
            ? 0
            : this.fullBarElement.dimension - this.ceilLabelElement.dimension),
          this.ceilLabelElement.setPosition(e));
      }),
      (z.prototype.updateHandles = function (e, i) {
        e === d.Min
          ? this.updateLowHandle(i)
          : e === d.Max && this.updateHighHandle(i),
          this.updateSelectionBar(),
          this.updateTicksScale(),
          this.range && this.updateCombinedLabel();
      }),
      (z.prototype.getHandleLabelPos = function (e, i) {
        var t = (
            e === d.Min
              ? this.minHandleLabelElement
              : this.maxHandleLabelElement
          ).dimension,
          i = i - t / 2 + this.handleHalfDimension,
          t = this.fullBarElement.dimension - t;
        return this.viewOptions.boundPointerLabels
          ? (this.viewOptions.rightToLeft && e === d.Min) ||
            (!this.viewOptions.rightToLeft && e === d.Max)
            ? Math.min(i, t)
            : Math.min(Math.max(i, 0), t)
          : i;
      }),
      (z.prototype.updateLowHandle = function (e) {
        this.minHandleElement.setPosition(e),
          this.minHandleLabelElement.setValue(
            this.getDisplayValue(this.viewLowValue, u.Low)
          ),
          this.minHandleLabelElement.setPosition(
            this.getHandleLabelPos(d.Min, e)
          ),
          g.isNullOrUndefined(this.viewOptions.getPointerColor) ||
            (this.minPointerStyle = {
              backgroundColor: this.getPointerColor(d.Min),
            }),
          this.viewOptions.autoHideLimitLabels &&
            this.updateFloorAndCeilLabelsVisibility();
      }),
      (z.prototype.updateHighHandle = function (e) {
        this.maxHandleElement.setPosition(e),
          this.maxHandleLabelElement.setValue(
            this.getDisplayValue(this.viewHighValue, u.High)
          ),
          this.maxHandleLabelElement.setPosition(
            this.getHandleLabelPos(d.Max, e)
          ),
          g.isNullOrUndefined(this.viewOptions.getPointerColor) ||
            (this.maxPointerStyle = {
              backgroundColor: this.getPointerColor(d.Max),
            }),
          this.viewOptions.autoHideLimitLabels &&
            this.updateFloorAndCeilLabelsVisibility();
      }),
      (z.prototype.updateFloorAndCeilLabelsVisibility = function () {
        var e, i, t, n, s, o, l;
        this.viewOptions.hidePointerLabels ||
          ((i = e = !1),
          (l = this.isLabelBelowFloorLabel(this.minHandleLabelElement)),
          (t = this.isLabelAboveCeilLabel(this.minHandleLabelElement)),
          (o = this.isLabelAboveCeilLabel(this.maxHandleLabelElement)),
          (n = this.isLabelBelowFloorLabel(this.combinedLabelElement)),
          (s = this.isLabelAboveCeilLabel(this.combinedLabelElement)),
          l
            ? ((e = !0), this.floorLabelElement.hide())
            : ((e = !1), this.floorLabelElement.show()),
          t
            ? ((i = !0), this.ceilLabelElement.hide())
            : ((i = !1), this.ceilLabelElement.show()),
          this.range &&
            ((o = this.combinedLabelElement.isVisible() ? s : o),
            (l = this.combinedLabelElement.isVisible() ? n : l),
            o
              ? this.ceilLabelElement.hide()
              : i || this.ceilLabelElement.show(),
            l
              ? this.floorLabelElement.hide()
              : e || this.floorLabelElement.show()));
      }),
      (z.prototype.isLabelBelowFloorLabel = function (e) {
        var i = e.position,
          t = e.dimension,
          n = this.floorLabelElement.position,
          e = this.floorLabelElement.dimension;
        return this.viewOptions.rightToLeft ? n - 2 <= i + t : i <= n + e + 2;
      }),
      (z.prototype.isLabelAboveCeilLabel = function (e) {
        var i = e.position,
          t = e.dimension,
          n = this.ceilLabelElement.position,
          e = this.ceilLabelElement.dimension;
        return this.viewOptions.rightToLeft ? i <= n + e + 2 : n - 2 <= i + t;
      }),
      (z.prototype.updateSelectionBar = function () {
        var e,
          i,
          t = 0,
          n = 0,
          s = this.viewOptions.rightToLeft
            ? !this.viewOptions.showSelectionBarEnd
            : this.viewOptions.showSelectionBarEnd,
          o = this.viewOptions.rightToLeft
            ? this.maxHandleElement.position + this.handleHalfDimension
            : this.minHandleElement.position + this.handleHalfDimension;
        (t = this.range
          ? ((n = Math.abs(
              this.maxHandleElement.position - this.minHandleElement.position
            )),
            o)
          : g.isNullOrUndefined(this.viewOptions.showSelectionBarFromValue)
          ? s
            ? ((n = Math.ceil(
                Math.abs(
                  this.maxHandlePosition - this.minHandleElement.position
                ) + this.handleHalfDimension
              )),
              Math.floor(
                this.minHandleElement.position + this.handleHalfDimension
              ))
            : ((n = this.minHandleElement.position + this.handleHalfDimension),
              0)
          : ((o = this.viewOptions.showSelectionBarFromValue),
            (e = this.valueToPosition(o)),
            (
              this.viewOptions.rightToLeft
                ? this.viewLowValue <= o
                : this.viewLowValue > o
            )
              ? ((n = this.minHandleElement.position - e),
                e + this.handleHalfDimension)
              : ((n = e - this.minHandleElement.position),
                this.minHandleElement.position + this.handleHalfDimension))),
          this.selectionBarElement.setDimension(n),
          this.selectionBarElement.setPosition(t),
          this.range &&
            this.viewOptions.showOuterSelectionBars &&
            (this.viewOptions.rightToLeft
              ? (this.rightOuterSelectionBarElement.setDimension(t),
                this.rightOuterSelectionBarElement.setPosition(0),
                this.fullBarElement.calculateDimension(),
                this.leftOuterSelectionBarElement.setDimension(
                  this.fullBarElement.dimension - (t + n)
                ),
                this.leftOuterSelectionBarElement.setPosition(t + n))
              : (this.leftOuterSelectionBarElement.setDimension(t),
                this.leftOuterSelectionBarElement.setPosition(0),
                this.fullBarElement.calculateDimension(),
                this.rightOuterSelectionBarElement.setDimension(
                  this.fullBarElement.dimension - (t + n)
                ),
                this.rightOuterSelectionBarElement.setPosition(t + n))),
          g.isNullOrUndefined(this.viewOptions.getSelectionBarColor)
            ? g.isNullOrUndefined(this.viewOptions.selectionBarGradient) ||
              ((i =
                (0 <
                  (e = g.isNullOrUndefined(
                    this.viewOptions.showSelectionBarFromValue
                  )
                    ? 0
                    : this.valueToPosition(
                        this.viewOptions.showSelectionBarFromValue
                      )) -
                    t &&
                  !s) ||
                (e - t <= 0 && s)),
              (s = this.viewOptions.vertical
                ? i
                  ? 'bottom'
                  : 'top'
                : i
                ? 'left'
                : 'right'),
              (this.barStyle = {
                backgroundImage:
                  'linear-gradient(to ' +
                  s +
                  ', ' +
                  this.viewOptions.selectionBarGradient.from +
                  ' 0%,' +
                  this.viewOptions.selectionBarGradient.to +
                  ' 100%)',
              }),
              this.viewOptions.vertical
                ? ((this.barStyle.backgroundPosition =
                    'center ' +
                    (e + n + t + (i ? -this.handleHalfDimension : 0)) +
                    'px'),
                  (this.barStyle.backgroundSize =
                    '100% ' +
                    (this.fullBarElement.dimension - this.handleHalfDimension) +
                    'px'))
                : ((this.barStyle.backgroundPosition =
                    e - t + (i ? this.handleHalfDimension : 0) + 'px center'),
                  (this.barStyle.backgroundSize =
                    this.fullBarElement.dimension -
                    this.handleHalfDimension +
                    'px 100%')))
            : ((i = this.getSelectionBarColor()),
              (this.barStyle = { backgroundColor: i }));
      }),
      (z.prototype.getSelectionBarColor = function () {
        return this.range
          ? this.viewOptions.getSelectionBarColor(this.value, this.highValue)
          : this.viewOptions.getSelectionBarColor(this.value);
      }),
      (z.prototype.getPointerColor = function (e) {
        return e === d.Max
          ? this.viewOptions.getPointerColor(this.highValue, e)
          : this.viewOptions.getPointerColor(this.value, e);
      }),
      (z.prototype.getTickColor = function (e) {
        return this.viewOptions.getTickColor(e);
      }),
      (z.prototype.updateCombinedLabel = function () {
        var e, i;
        (
          this.viewOptions.rightToLeft
            ? this.minHandleLabelElement.position -
                this.minHandleLabelElement.dimension -
                10 <=
              this.maxHandleLabelElement.position
            : this.minHandleLabelElement.position +
                this.minHandleLabelElement.dimension +
                10 >=
              this.maxHandleLabelElement.position
        )
          ? ((e = this.getDisplayValue(this.viewLowValue, u.Low)),
            (i = this.getDisplayValue(this.viewHighValue, u.High)),
            (i = this.viewOptions.rightToLeft
              ? this.viewOptions.combineLabels(i, e)
              : this.viewOptions.combineLabels(e, i)),
            this.combinedLabelElement.setValue(i),
            (i = this.viewOptions.boundPointerLabels
              ? Math.min(
                  Math.max(
                    this.selectionBarElement.position +
                      this.selectionBarElement.dimension / 2 -
                      this.combinedLabelElement.dimension / 2,
                    0
                  ),
                  this.fullBarElement.dimension -
                    this.combinedLabelElement.dimension
                )
              : this.selectionBarElement.position +
                this.selectionBarElement.dimension / 2 -
                this.combinedLabelElement.dimension / 2),
            this.combinedLabelElement.setPosition(i),
            this.minHandleLabelElement.hide(),
            this.maxHandleLabelElement.hide(),
            this.combinedLabelElement.show())
          : (this.updateHighHandle(this.valueToPosition(this.viewHighValue)),
            this.updateLowHandle(this.valueToPosition(this.viewLowValue)),
            this.maxHandleLabelElement.show(),
            this.minHandleLabelElement.show(),
            this.combinedLabelElement.hide()),
          this.viewOptions.autoHideLimitLabels &&
            this.updateFloorAndCeilLabelsVisibility();
      }),
      (z.prototype.getDisplayValue = function (e, i) {
        return (
          g.isNullOrUndefined(this.viewOptions.stepsArray) ||
            this.viewOptions.bindIndexForStepsArray ||
            (e = this.getStepValue(e)),
          this.viewOptions.translate(e, i)
        );
      }),
      (z.prototype.roundStep = function (e, i) {
        (i = g.isNullOrUndefined(i) ? this.viewOptions.step : i),
          (e = w.roundToPrecisionLimit(
            (e - this.viewOptions.floor) / i,
            this.viewOptions.precisionLimit
          )),
          (e = Math.round(e) * i);
        return w.roundToPrecisionLimit(
          this.viewOptions.floor + e,
          this.viewOptions.precisionLimit
        );
      }),
      (z.prototype.valueToPosition = function (e) {
        var i = g.linearValueToPosition;
        g.isNullOrUndefined(this.viewOptions.customValueToPosition)
          ? this.viewOptions.logScale && (i = g.logValueToPosition)
          : (i = this.viewOptions.customValueToPosition);
        e = i(
          (e = w.clampToRange(
            e,
            this.viewOptions.floor,
            this.viewOptions.ceil
          )),
          this.viewOptions.floor,
          this.viewOptions.ceil
        );
        return (
          g.isNullOrUndefined(e) && (e = 0),
          this.viewOptions.rightToLeft && (e = 1 - e),
          e * this.maxHandlePosition
        );
      }),
      (z.prototype.positionToValue = function (e) {
        var i = e / this.maxHandlePosition;
        this.viewOptions.rightToLeft && (i = 1 - i);
        e = g.linearPositionToValue;
        g.isNullOrUndefined(this.viewOptions.customPositionToValue)
          ? this.viewOptions.logScale && (e = g.logPositionToValue)
          : (e = this.viewOptions.customPositionToValue);
        i = e(i, this.viewOptions.floor, this.viewOptions.ceil);
        return g.isNullOrUndefined(i) ? 0 : i;
      }),
      (z.prototype.getEventXY = function (e, i) {
        if (e instanceof MouseEvent)
          return this.viewOptions.vertical || 0 !== this.viewOptions.rotate
            ? e.clientY
            : e.clientX;
        var t = 0,
          n = e.touches;
        if (!g.isNullOrUndefined(i))
          for (var s = 0; s < n.length; s++)
            if (n[s].identifier === i) {
              t = s;
              break;
            }
        return this.viewOptions.vertical || 0 !== this.viewOptions.rotate
          ? n[t].clientY
          : n[t].clientX;
      }),
      (z.prototype.getEventPosition = function (e, i) {
        var t = this.elementRef.nativeElement.getBoundingClientRect(),
          t =
            this.viewOptions.vertical || 0 !== this.viewOptions.rotate
              ? t.bottom
              : t.left;
        return (
          (this.viewOptions.vertical || 0 !== this.viewOptions.rotate
            ? -this.getEventXY(e, i) + t
            : this.getEventXY(e, i) - t) *
            this.viewOptions.scale -
          this.handleHalfDimension
        );
      }),
      (z.prototype.getNearestHandle = function (e) {
        if (!this.range) return d.Min;
        var i = this.getEventPosition(e),
          t = Math.abs(i - this.minHandleElement.position),
          e = Math.abs(i - this.maxHandleElement.position);
        return t < e
          ? d.Min
          : e < t
          ? d.Max
          : this.viewOptions.rightToLeft
          ? i > this.minHandleElement.position
            ? d.Min
            : d.Max
          : i < this.minHandleElement.position
          ? d.Min
          : d.Max;
      }),
      (z.prototype.bindEvents = function () {
        var i = this,
          t = this.viewOptions.draggableRange;
        this.viewOptions.onlyBindHandles ||
          this.selectionBarElement.on('mousedown', function (e) {
            return i.onBarStart(null, t, e, !0, !0, !0);
          }),
          this.viewOptions.draggableRangeOnly
            ? (this.minHandleElement.on('mousedown', function (e) {
                return i.onBarStart(d.Min, t, e, !0, !0);
              }),
              this.maxHandleElement.on('mousedown', function (e) {
                return i.onBarStart(d.Max, t, e, !0, !0);
              }))
            : (this.minHandleElement.on('mousedown', function (e) {
                return i.onStart(d.Min, e, !0, !0);
              }),
              this.range &&
                this.maxHandleElement.on('mousedown', function (e) {
                  return i.onStart(d.Max, e, !0, !0);
                }),
              this.viewOptions.onlyBindHandles ||
                (this.fullBarElement.on('mousedown', function (e) {
                  return i.onStart(null, e, !0, !0, !0);
                }),
                this.ticksElement.on('mousedown', function (e) {
                  return i.onStart(null, e, !0, !0, !0, !0);
                }))),
          this.viewOptions.onlyBindHandles ||
            this.selectionBarElement.onPassive('touchstart', function (e) {
              return i.onBarStart(null, t, e, !0, !0, !0);
            }),
          this.viewOptions.draggableRangeOnly
            ? (this.minHandleElement.onPassive('touchstart', function (e) {
                return i.onBarStart(d.Min, t, e, !0, !0);
              }),
              this.maxHandleElement.onPassive('touchstart', function (e) {
                return i.onBarStart(d.Max, t, e, !0, !0);
              }))
            : (this.minHandleElement.onPassive('touchstart', function (e) {
                return i.onStart(d.Min, e, !0, !0);
              }),
              this.range &&
                this.maxHandleElement.onPassive('touchstart', function (e) {
                  return i.onStart(d.Max, e, !0, !0);
                }),
              this.viewOptions.onlyBindHandles ||
                (this.fullBarElement.onPassive('touchstart', function (e) {
                  return i.onStart(null, e, !0, !0, !0);
                }),
                this.ticksElement.onPassive('touchstart', function (e) {
                  return i.onStart(null, e, !1, !1, !0, !0);
                }))),
          this.viewOptions.keyboardSupport &&
            (this.minHandleElement.on('focus', function () {
              return i.onPointerFocus(d.Min);
            }),
            this.range &&
              this.maxHandleElement.on('focus', function () {
                return i.onPointerFocus(d.Max);
              }));
      }),
      (z.prototype.getOptionsInfluencingEventBindings = function (e) {
        return [
          e.disabled,
          e.readOnly,
          e.draggableRange,
          e.draggableRangeOnly,
          e.onlyBindHandles,
          e.keyboardSupport,
        ];
      }),
      (z.prototype.unbindEvents = function () {
        this.unsubscribeOnMove(), this.unsubscribeOnEnd();
        try {
          for (
            var e = h(this.getAllSliderElements()), i = e.next();
            !i.done;
            i = e.next()
          ) {
            var t = i.value;
            g.isNullOrUndefined(t) || t.off();
          }
        } catch (o) {
          n = { error: o };
        } finally {
          try {
            i && !i.done && (s = e['return']) && s.call(e);
          } finally {
            if (n) throw n.error;
          }
        }
        var n, s;
      }),
      (z.prototype.onBarStart = function (e, i, t, n, s, o, l) {
        i ? this.onDragStart(e, t, n, s) : this.onStart(e, t, n, s, o, l);
      }),
      (z.prototype.onStart = function (e, i, t, n, s, o) {
        var l = this;
        i.stopPropagation(),
          m.isTouchEvent(i) || r.supportsPassiveEvents || i.preventDefault(),
          (this.moving = !1),
          this.calculateViewDimensions(),
          g.isNullOrUndefined(e) && (e = this.getNearestHandle(i)),
          (this.currentTrackingPointer = e);
        e = this.getPointerElement(e);
        (e.active = !0),
          this.viewOptions.keyboardSupport && e.focus(),
          t &&
            (this.unsubscribeOnMove(),
            (t = function (e) {
              return l.dragging.active ? l.onDragMove(e) : l.onMove(e);
            }),
            m.isTouchEvent(i)
              ? (this.onMoveEventListener =
                  this.eventListenerHelper.attachPassiveEventListener(
                    document,
                    'touchmove',
                    t
                  ))
              : (this.onMoveEventListener =
                  this.eventListenerHelper.attachEventListener(
                    document,
                    'mousemove',
                    t
                  ))),
          n &&
            (this.unsubscribeOnEnd(),
            (n = function (e) {
              return l.onEnd(e);
            }),
            m.isTouchEvent(i)
              ? (this.onEndEventListener =
                  this.eventListenerHelper.attachPassiveEventListener(
                    document,
                    'touchend',
                    n
                  ))
              : (this.onEndEventListener =
                  this.eventListenerHelper.attachEventListener(
                    document,
                    'mouseup',
                    n
                  ))),
          this.userChangeStart.emit(this.getChangeContext()),
          m.isTouchEvent(i) &&
            !g.isNullOrUndefined(i.changedTouches) &&
            g.isNullOrUndefined(this.touchId) &&
            (this.touchId = i.changedTouches[0].identifier),
          s && this.onMove(i, !0),
          o && this.onEnd(i);
      }),
      (z.prototype.onMove = function (e, i) {
        var t = null;
        if (m.isTouchEvent(e)) {
          for (var n = e.changedTouches, s = 0; s < n.length; s++)
            if (n[s].identifier === this.touchId) {
              t = n[s];
              break;
            }
          if (g.isNullOrUndefined(t)) return;
        }
        this.viewOptions.animate &&
          !this.viewOptions.animateOnMove &&
          this.moving &&
          (this.sliderElementAnimateClass = !1),
          (this.moving = !0);
        var o = g.isNullOrUndefined(t)
            ? this.getEventPosition(e)
            : this.getEventPosition(e, t.identifier),
          l = this.viewOptions.rightToLeft
            ? this.viewOptions.floor
            : this.viewOptions.ceil,
          e = this.viewOptions.rightToLeft
            ? this.viewOptions.ceil
            : this.viewOptions.floor,
          a =
            o <= 0
              ? e
              : o >= this.maxHandlePosition
              ? l
              : ((a = this.positionToValue(o)),
                i && !g.isNullOrUndefined(this.viewOptions.tickStep)
                  ? this.roundStep(a, this.viewOptions.tickStep)
                  : this.roundStep(a));
        this.positionTrackingHandle(a);
      }),
      (z.prototype.onEnd = function (e) {
        if (
          m.isTouchEvent(e) &&
          e.changedTouches[0].identifier !== this.touchId
        )
          return;
        (this.moving = !1),
          this.viewOptions.animate && (this.sliderElementAnimateClass = !0),
          (this.touchId = null),
          this.viewOptions.keyboardSupport ||
            ((this.minHandleElement.active = !1),
            (this.maxHandleElement.active = !1),
            (this.currentTrackingPointer = null)),
          (this.dragging.active = !1),
          this.unsubscribeOnMove(),
          this.unsubscribeOnEnd(),
          this.userChangeEnd.emit(this.getChangeContext());
      }),
      (z.prototype.onPointerFocus = function (e) {
        var i = this,
          t = this.getPointerElement(e);
        t.on('blur', function () {
          return i.onPointerBlur(t);
        }),
          t.on('keydown', function (e) {
            return i.onKeyboardEvent(e);
          }),
          t.on('keyup', function () {
            return i.onKeyUp();
          }),
          (t.active = !0),
          (this.currentTrackingPointer = e),
          (this.currentFocusPointer = e),
          (this.firstKeyDown = !0);
      }),
      (z.prototype.onKeyUp = function () {
        (this.firstKeyDown = !0),
          this.userChangeEnd.emit(this.getChangeContext());
      }),
      (z.prototype.onPointerBlur = function (e) {
        e.off('blur'),
          e.off('keydown'),
          e.off('keyup'),
          (e.active = !1),
          g.isNullOrUndefined(this.touchId) &&
            ((this.currentTrackingPointer = null),
            (this.currentFocusPointer = null));
      }),
      (z.prototype.getKeyActions = function (e) {
        var i = this.viewOptions.ceil - this.viewOptions.floor,
          t = e + this.viewOptions.step,
          n = e - this.viewOptions.step,
          s = e + i / 10,
          o = e - i / 10;
        this.viewOptions.reversedControls &&
          ((t = e - this.viewOptions.step),
          (n = e + this.viewOptions.step),
          (s = e - i / 10),
          (o = e + i / 10));
        o = {
          UP: t,
          DOWN: n,
          LEFT: n,
          RIGHT: t,
          PAGEUP: s,
          PAGEDOWN: o,
          HOME: this.viewOptions.reversedControls
            ? this.viewOptions.ceil
            : this.viewOptions.floor,
          END: this.viewOptions.reversedControls
            ? this.viewOptions.floor
            : this.viewOptions.ceil,
        };
        return (
          this.viewOptions.rightToLeft &&
            ((o.LEFT = t),
            (o.RIGHT = n),
            (!this.viewOptions.vertical && 0 === this.viewOptions.rotate) ||
              ((o.UP = n), (o.DOWN = t))),
          o
        );
      }),
      (z.prototype.onKeyboardEvent = function (e) {
        var i = this.getCurrentTrackingValue(),
          t = g.isNullOrUndefined(e.keyCode) ? e.which : e.keyCode,
          n =
            this.getKeyActions(i)[
              {
                38: 'UP',
                40: 'DOWN',
                37: 'LEFT',
                39: 'RIGHT',
                33: 'PAGEUP',
                34: 'PAGEDOWN',
                36: 'HOME',
                35: 'END',
              }[t]
            ];
        g.isNullOrUndefined(n) ||
          g.isNullOrUndefined(this.currentTrackingPointer) ||
          (e.preventDefault(),
          this.firstKeyDown &&
            ((this.firstKeyDown = !1),
            this.userChangeStart.emit(this.getChangeContext())),
          (i = w.clampToRange(
            n,
            this.viewOptions.floor,
            this.viewOptions.ceil
          )),
          (t = this.roundStep(i)),
          this.viewOptions.draggableRangeOnly
            ? ((e = this.viewHighValue - this.viewLowValue),
              (i = n = void 0),
              this.currentTrackingPointer === d.Min
                ? (i = (n = t) + e) > this.viewOptions.ceil &&
                  (n = (i = this.viewOptions.ceil) - e)
                : this.currentTrackingPointer === d.Max &&
                  (n = (i = t) - e) < this.viewOptions.floor &&
                  (i = (n = this.viewOptions.floor) + e),
              this.positionTrackingBar(n, i))
            : this.positionTrackingHandle(t));
      }),
      (z.prototype.onDragStart = function (e, i, t, n) {
        var s = this.getEventPosition(i);
        (this.dragging = new k()),
          (this.dragging.active = !0),
          (this.dragging.value = this.positionToValue(s)),
          (this.dragging.difference = this.viewHighValue - this.viewLowValue),
          (this.dragging.lowLimit = this.viewOptions.rightToLeft
            ? this.minHandleElement.position - s
            : s - this.minHandleElement.position),
          (this.dragging.highLimit = this.viewOptions.rightToLeft
            ? s - this.maxHandleElement.position
            : this.maxHandleElement.position - s),
          this.onStart(e, i, t, n);
      }),
      (z.prototype.getMinValue = function (e, i, t) {
        var n = this.viewOptions.rightToLeft,
          s = null,
          s = i
            ? t
              ? n
                ? this.viewOptions.floor
                : this.viewOptions.ceil - this.dragging.difference
              : n
              ? this.viewOptions.ceil - this.dragging.difference
              : this.viewOptions.floor
            : n
            ? this.positionToValue(e + this.dragging.lowLimit)
            : this.positionToValue(e - this.dragging.lowLimit);
        return this.roundStep(s);
      }),
      (z.prototype.getMaxValue = function (e, i, t) {
        var n = this.viewOptions.rightToLeft,
          s = null,
          s = i
            ? t
              ? n
                ? this.viewOptions.floor + this.dragging.difference
                : this.viewOptions.ceil
              : n
              ? this.viewOptions.ceil
              : this.viewOptions.floor + this.dragging.difference
            : n
            ? this.positionToValue(e + this.dragging.lowLimit) +
              this.dragging.difference
            : this.positionToValue(e - this.dragging.lowLimit) +
              this.dragging.difference;
        return this.roundStep(s);
      }),
      (z.prototype.onDragMove = function (e) {
        var i,
          t = this.getEventPosition(e);
        this.viewOptions.animate &&
          !this.viewOptions.animateOnMove &&
          this.moving &&
          (this.sliderElementAnimateClass = !1),
          (this.moving = !0),
          (e = this.viewOptions.rightToLeft
            ? ((l = this.dragging.lowLimit),
              (o = this.dragging.highLimit),
              (i = this.maxHandleElement),
              this.minHandleElement)
            : ((l = this.dragging.highLimit),
              (o = this.dragging.lowLimit),
              (i = this.minHandleElement),
              this.maxHandleElement));
        var n,
          s,
          o = t <= o,
          l = t >= this.maxHandlePosition - l;
        if (o) {
          if (0 === i.position) return;
          (n = this.getMinValue(t, !0, !1)), (s = this.getMaxValue(t, !0, !1));
        } else if (l) {
          if (e.position === this.maxHandlePosition) return;
          (s = this.getMaxValue(t, !0, !0)), (n = this.getMinValue(t, !0, !0));
        } else
          (n = this.getMinValue(t, !1, !1)), (s = this.getMaxValue(t, !1, !1));
        this.positionTrackingBar(n, s);
      }),
      (z.prototype.positionTrackingBar = function (e, i) {
        !g.isNullOrUndefined(this.viewOptions.minLimit) &&
          e < this.viewOptions.minLimit &&
          ((e = this.viewOptions.minLimit),
          (i = w.roundToPrecisionLimit(
            e + this.dragging.difference,
            this.viewOptions.precisionLimit
          ))),
          !g.isNullOrUndefined(this.viewOptions.maxLimit) &&
            i > this.viewOptions.maxLimit &&
            ((i = this.viewOptions.maxLimit),
            (e = w.roundToPrecisionLimit(
              i - this.dragging.difference,
              this.viewOptions.precisionLimit
            ))),
          (this.viewLowValue = e),
          (this.viewHighValue = i),
          this.applyViewChange(),
          this.updateHandles(d.Min, this.valueToPosition(e)),
          this.updateHandles(d.Max, this.valueToPosition(i));
      }),
      (z.prototype.positionTrackingHandle = function (e) {
        (e = this.applyMinMaxLimit(e)),
          this.range &&
            (this.viewOptions.pushRange
              ? (e = this.applyPushRange(e))
              : (this.viewOptions.noSwitching &&
                  (this.currentTrackingPointer === d.Min &&
                  e > this.viewHighValue
                    ? (e = this.applyMinMaxRange(this.viewHighValue))
                    : this.currentTrackingPointer === d.Max &&
                      e < this.viewLowValue &&
                      (e = this.applyMinMaxRange(this.viewLowValue))),
                (e = this.applyMinMaxRange(e)),
                this.currentTrackingPointer === d.Min && e > this.viewHighValue
                  ? ((this.viewLowValue = this.viewHighValue),
                    this.applyViewChange(),
                    this.updateHandles(d.Min, this.maxHandleElement.position),
                    this.updateAriaAttributes(),
                    (this.currentTrackingPointer = d.Max),
                    (this.minHandleElement.active = !1),
                    (this.maxHandleElement.active = !0),
                    this.viewOptions.keyboardSupport &&
                      this.maxHandleElement.focus())
                  : this.currentTrackingPointer === d.Max &&
                    e < this.viewLowValue &&
                    ((this.viewHighValue = this.viewLowValue),
                    this.applyViewChange(),
                    this.updateHandles(d.Max, this.minHandleElement.position),
                    this.updateAriaAttributes(),
                    (this.currentTrackingPointer = d.Min),
                    (this.maxHandleElement.active = !1),
                    (this.minHandleElement.active = !0),
                    this.viewOptions.keyboardSupport &&
                      this.minHandleElement.focus()))),
          this.getCurrentTrackingValue() !== e &&
            (this.currentTrackingPointer === d.Min
              ? ((this.viewLowValue = e), this.applyViewChange())
              : this.currentTrackingPointer === d.Max &&
                ((this.viewHighValue = e), this.applyViewChange()),
            this.updateHandles(
              this.currentTrackingPointer,
              this.valueToPosition(e)
            ),
            this.updateAriaAttributes());
      }),
      (z.prototype.applyMinMaxLimit = function (e) {
        return !g.isNullOrUndefined(this.viewOptions.minLimit) &&
          e < this.viewOptions.minLimit
          ? this.viewOptions.minLimit
          : !g.isNullOrUndefined(this.viewOptions.maxLimit) &&
            e > this.viewOptions.maxLimit
          ? this.viewOptions.maxLimit
          : e;
      }),
      (z.prototype.applyMinMaxRange = function (e) {
        var i =
            this.currentTrackingPointer === d.Min
              ? this.viewHighValue
              : this.viewLowValue,
          i = Math.abs(e - i);
        if (
          !g.isNullOrUndefined(this.viewOptions.minRange) &&
          i < this.viewOptions.minRange
        ) {
          if (this.currentTrackingPointer === d.Min)
            return w.roundToPrecisionLimit(
              this.viewHighValue - this.viewOptions.minRange,
              this.viewOptions.precisionLimit
            );
          if (this.currentTrackingPointer === d.Max)
            return w.roundToPrecisionLimit(
              this.viewLowValue + this.viewOptions.minRange,
              this.viewOptions.precisionLimit
            );
        }
        if (
          !g.isNullOrUndefined(this.viewOptions.maxRange) &&
          i > this.viewOptions.maxRange
        ) {
          if (this.currentTrackingPointer === d.Min)
            return w.roundToPrecisionLimit(
              this.viewHighValue - this.viewOptions.maxRange,
              this.viewOptions.precisionLimit
            );
          if (this.currentTrackingPointer === d.Max)
            return w.roundToPrecisionLimit(
              this.viewLowValue + this.viewOptions.maxRange,
              this.viewOptions.precisionLimit
            );
        }
        return e;
      }),
      (z.prototype.applyPushRange = function (e) {
        var i =
            this.currentTrackingPointer === d.Min
              ? this.viewHighValue - e
              : e - this.viewLowValue,
          t = g.isNullOrUndefined(this.viewOptions.minRange)
            ? this.viewOptions.step
            : this.viewOptions.minRange,
          n = this.viewOptions.maxRange;
        return (
          i < t
            ? (this.currentTrackingPointer === d.Min
                ? ((this.viewHighValue = w.roundToPrecisionLimit(
                    Math.min(e + t, this.viewOptions.ceil),
                    this.viewOptions.precisionLimit
                  )),
                  (e = w.roundToPrecisionLimit(
                    this.viewHighValue - t,
                    this.viewOptions.precisionLimit
                  )),
                  this.applyViewChange(),
                  this.updateHandles(
                    d.Max,
                    this.valueToPosition(this.viewHighValue)
                  ))
                : this.currentTrackingPointer === d.Max &&
                  ((this.viewLowValue = w.roundToPrecisionLimit(
                    Math.max(e - t, this.viewOptions.floor),
                    this.viewOptions.precisionLimit
                  )),
                  (e = w.roundToPrecisionLimit(
                    this.viewLowValue + t,
                    this.viewOptions.precisionLimit
                  )),
                  this.applyViewChange(),
                  this.updateHandles(
                    d.Min,
                    this.valueToPosition(this.viewLowValue)
                  )),
              this.updateAriaAttributes())
            : !g.isNullOrUndefined(n) &&
              n < i &&
              (this.currentTrackingPointer === d.Min
                ? ((this.viewHighValue = w.roundToPrecisionLimit(
                    e + n,
                    this.viewOptions.precisionLimit
                  )),
                  this.applyViewChange(),
                  this.updateHandles(
                    d.Max,
                    this.valueToPosition(this.viewHighValue)
                  ))
                : this.currentTrackingPointer === d.Max &&
                  ((this.viewLowValue = w.roundToPrecisionLimit(
                    e - n,
                    this.viewOptions.precisionLimit
                  )),
                  this.applyViewChange(),
                  this.updateHandles(
                    d.Min,
                    this.valueToPosition(this.viewLowValue)
                  )),
              this.updateAriaAttributes()),
          e
        );
      }),
      (z.prototype.getChangeContext = function () {
        var e = new c();
        return (
          (e.pointerType = this.currentTrackingPointer),
          (e.value = +this.value),
          this.range && (e.highValue = +this.highValue),
          e
        );
      }),
      (z.decorators = [
        {
          type: s.Component,
          args: [
            {
              selector: 'ngx-slider',
              template:
                '\x3c!-- // 0 Left selection bar outside two handles --\x3e\n<span ngxSliderElement #leftOuterSelectionBar class="ngx-slider-span ngx-slider-bar-wrapper ngx-slider-left-out-selection">\n  <span class="ngx-slider-span ngx-slider-bar"></span>\n</span>\n\x3c!-- // 1 Right selection bar outside two handles --\x3e\n<span ngxSliderElement #rightOuterSelectionBar class="ngx-slider-span ngx-slider-bar-wrapper ngx-slider-right-out-selection">\n  <span class="ngx-slider-span ngx-slider-bar"></span>\n</span>\n\x3c!-- // 2 The whole slider bar --\x3e\n<span ngxSliderElement #fullBar [class.ngx-slider-transparent]="fullBarTransparentClass" class="ngx-slider-span ngx-slider-bar-wrapper ngx-slider-full-bar">\n  <span class="ngx-slider-span ngx-slider-bar"></span>\n</span>\n\x3c!-- // 3 Selection bar between two handles --\x3e\n<span ngxSliderElement #selectionBar [class.ngx-slider-draggable]="selectionBarDraggableClass" class="ngx-slider-span ngx-slider-bar-wrapper ngx-slider-selection-bar">\n  <span class="ngx-slider-span ngx-slider-bar ngx-slider-selection" [ngStyle]="barStyle"></span>\n</span>\n\x3c!-- // 4 Low slider handle --\x3e\n<span ngxSliderHandle #minHandle class="ngx-slider-span ngx-slider-pointer ngx-slider-pointer-min" [ngStyle]=minPointerStyle></span>\n\x3c!-- // 5 High slider handle --\x3e\n<span ngxSliderHandle #maxHandle [style.display]="range ? \'inherit\' : \'none\'" class="ngx-slider-span ngx-slider-pointer ngx-slider-pointer-max" [ngStyle]=maxPointerStyle></span>\n\x3c!-- // 6 Floor label --\x3e\n<span ngxSliderLabel #floorLabel class="ngx-slider-span ngx-slider-bubble ngx-slider-limit ngx-slider-floor"></span>\n\x3c!-- // 7 Ceiling label --\x3e\n<span ngxSliderLabel #ceilLabel class="ngx-slider-span ngx-slider-bubble ngx-slider-limit ngx-slider-ceil"></span>\n\x3c!-- // 8 Label above the low slider handle --\x3e\n<span ngxSliderLabel #minHandleLabel class="ngx-slider-span ngx-slider-bubble ngx-slider-model-value"></span>\n\x3c!-- // 9 Label above the high slider handle --\x3e\n<span ngxSliderLabel #maxHandleLabel class="ngx-slider-span ngx-slider-bubble ngx-slider-model-high"></span>\n\x3c!-- // 10 Combined range label when the slider handles are close ex. 15 - 17 --\x3e\n<span ngxSliderLabel #combinedLabel class="ngx-slider-span ngx-slider-bubble ngx-slider-combined"></span>\n\x3c!-- // 11 The ticks --\x3e\n<span ngxSliderElement #ticksElement [hidden]="!showTicks" [class.ngx-slider-ticks-values-under]="ticksUnderValuesClass" class="ngx-slider-ticks">\n  <span *ngFor="let t of ticks" class="ngx-slider-tick" [ngClass]="{\'ngx-slider-selected\': t.selected}" [ngStyle]="t.style">\n    <ngx-slider-tooltip-wrapper [template]="tooltipTemplate" [tooltip]="t.tooltip" [placement]="t.tooltipPlacement"></ngx-slider-tooltip-wrapper>\n    <ngx-slider-tooltip-wrapper *ngIf="t.value != null" class="ngx-slider-span ngx-slider-tick-value"\n        [template]="tooltipTemplate" [tooltip]="t.valueTooltip" [placement]="t.valueTooltipPlacement" [content]="t.value"></ngx-slider-tooltip-wrapper>\n    <span *ngIf="t.legend != null" class="ngx-slider-span ngx-slider-tick-legend" [innerHTML]="t.legend"></span>\n  </span>\n</span>',
              styles: [
                "::ng-deep .ngx-slider{display:inline-block;position:relative;height:4px;width:100%;margin:35px 0 15px;vertical-align:middle;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none;touch-action:pan-y}::ng-deep .ngx-slider.with-legend{margin-bottom:40px}::ng-deep .ngx-slider[disabled]{cursor:not-allowed}::ng-deep .ngx-slider[disabled] .ngx-slider-pointer{cursor:not-allowed;background-color:#d8e0f3}::ng-deep .ngx-slider[disabled] .ngx-slider-draggable{cursor:not-allowed}::ng-deep .ngx-slider[disabled] .ngx-slider-selection{background:#8b91a2}::ng-deep .ngx-slider[disabled] .ngx-slider-tick{cursor:not-allowed}::ng-deep .ngx-slider[disabled] .ngx-slider-tick.ngx-slider-selected{background:#8b91a2}::ng-deep .ngx-slider .ngx-slider-span{white-space:nowrap;position:absolute;display:inline-block}::ng-deep .ngx-slider .ngx-slider-base{width:100%;height:100%;padding:0}::ng-deep .ngx-slider .ngx-slider-bar-wrapper{left:0;box-sizing:border-box;margin-top:-16px;padding-top:16px;width:100%;height:32px;z-index:1}::ng-deep .ngx-slider .ngx-slider-draggable{cursor:move}::ng-deep .ngx-slider .ngx-slider-bar{left:0;width:100%;height:4px;z-index:1;background:#d8e0f3;border-radius:2px}::ng-deep .ngx-slider .ngx-slider-bar-wrapper.ngx-slider-transparent .ngx-slider-bar{background:0 0}::ng-deep .ngx-slider .ngx-slider-bar-wrapper.ngx-slider-left-out-selection .ngx-slider-bar{background:#df002d}::ng-deep .ngx-slider .ngx-slider-bar-wrapper.ngx-slider-right-out-selection .ngx-slider-bar{background:#03a688}::ng-deep .ngx-slider .ngx-slider-selection{z-index:2;background:#0db9f0;border-radius:2px}::ng-deep .ngx-slider .ngx-slider-pointer{cursor:pointer;width:32px;height:32px;top:-14px;background-color:#0db9f0;z-index:3;border-radius:16px}::ng-deep .ngx-slider .ngx-slider-pointer:after{content:'';width:8px;height:8px;position:absolute;top:12px;left:12px;border-radius:4px;background:#fff}::ng-deep .ngx-slider .ngx-slider-pointer:hover:after{background-color:#fff}::ng-deep .ngx-slider .ngx-slider-pointer.ngx-slider-active{z-index:4}::ng-deep .ngx-slider .ngx-slider-pointer.ngx-slider-active:after{background-color:#451aff}::ng-deep .ngx-slider .ngx-slider-bubble{cursor:default;bottom:16px;padding:1px 3px;color:#55637d;font-size:16px}::ng-deep .ngx-slider .ngx-slider-bubble.ngx-slider-limit{color:#55637d}::ng-deep .ngx-slider .ngx-slider-ticks{box-sizing:border-box;width:100%;height:0;position:absolute;left:0;top:-3px;margin:0;z-index:1;list-style:none}::ng-deep .ngx-slider .ngx-slider-ticks-values-under .ngx-slider-tick-value{top:auto;bottom:-36px}::ng-deep .ngx-slider .ngx-slider-tick{text-align:center;cursor:pointer;width:10px;height:10px;background:#d8e0f3;border-radius:50%;position:absolute;top:0;left:0;margin-left:11px}::ng-deep .ngx-slider .ngx-slider-tick.ngx-slider-selected{background:#0db9f0}::ng-deep .ngx-slider .ngx-slider-tick-value{position:absolute;top:-34px;-webkit-transform:translate(-50%,0);transform:translate(-50%,0)}::ng-deep .ngx-slider .ngx-slider-tick-legend{position:absolute;top:24px;-webkit-transform:translate(-50%,0);transform:translate(-50%,0);max-width:50px;white-space:normal}::ng-deep .ngx-slider.vertical{position:relative;width:4px;height:100%;margin:0 20px;padding:0;vertical-align:baseline;touch-action:pan-x}::ng-deep .ngx-slider.vertical .ngx-slider-base{width:100%;height:100%;padding:0}::ng-deep .ngx-slider.vertical .ngx-slider-bar-wrapper{top:auto;left:0;margin:0 0 0 -16px;padding:0 0 0 16px;height:100%;width:32px}::ng-deep .ngx-slider.vertical .ngx-slider-bar{bottom:0;left:auto;width:4px;height:100%}::ng-deep .ngx-slider.vertical .ngx-slider-pointer{left:-14px!important;top:auto;bottom:0}::ng-deep .ngx-slider.vertical .ngx-slider-bubble{left:16px!important;bottom:0}::ng-deep .ngx-slider.vertical .ngx-slider-ticks{height:100%;width:0;left:-3px;top:0;z-index:1}::ng-deep .ngx-slider.vertical .ngx-slider-tick{vertical-align:middle;margin-left:auto;margin-top:11px}::ng-deep .ngx-slider.vertical .ngx-slider-tick-value{left:24px;top:auto;-webkit-transform:translate(0,-28%);transform:translate(0,-28%)}::ng-deep .ngx-slider.vertical .ngx-slider-tick-legend{top:auto;right:24px;-webkit-transform:translate(0,-28%);transform:translate(0,-28%);max-width:none;white-space:nowrap}::ng-deep .ngx-slider.vertical .ngx-slider-ticks-values-under .ngx-slider-tick-value{bottom:auto;left:auto;right:24px}::ng-deep .ngx-slider *{transition:none}::ng-deep .ngx-slider.animate .ngx-slider-bar-wrapper{transition:.3s linear}::ng-deep .ngx-slider.animate .ngx-slider-selection{transition:background-color .3s linear}::ng-deep .ngx-slider.animate .ngx-slider-pointer{transition:.3s linear}::ng-deep .ngx-slider.animate .ngx-slider-pointer:after{transition:.3s linear}::ng-deep .ngx-slider.animate .ngx-slider-bubble{transition:.3s linear}::ng-deep .ngx-slider.animate .ngx-slider-bubble.ngx-slider-limit{transition:opacity .3s linear}::ng-deep .ngx-slider.animate .ngx-slider-bubble.ngx-slider-combined{transition:opacity .3s linear}::ng-deep .ngx-slider.animate .ngx-slider-tick{transition:background-color .3s linear}",
              ],
              host: { class: 'ngx-slider' },
              providers: [i],
            },
          ],
        },
      ]),
      (z.ctorParameters = function () {
        return [
          { type: s.Renderer2 },
          { type: s.ElementRef },
          { type: s.ChangeDetectorRef },
          { type: s.NgZone },
        ];
      }),
      (z.propDecorators = {
        value: [{ type: s.Input }],
        valueChange: [{ type: s.Output }],
        highValue: [{ type: s.Input }],
        highValueChange: [{ type: s.Output }],
        options: [{ type: s.Input }],
        userChangeStart: [{ type: s.Output }],
        userChange: [{ type: s.Output }],
        userChangeEnd: [{ type: s.Output }],
        manualRefresh: [{ type: s.Input }],
        triggerFocus: [{ type: s.Input }],
        leftOuterSelectionBarElement: [
          { type: s.ViewChild, args: ['leftOuterSelectionBar', { read: L }] },
        ],
        rightOuterSelectionBarElement: [
          { type: s.ViewChild, args: ['rightOuterSelectionBar', { read: L }] },
        ],
        fullBarElement: [{ type: s.ViewChild, args: ['fullBar', { read: L }] }],
        selectionBarElement: [
          { type: s.ViewChild, args: ['selectionBar', { read: L }] },
        ],
        minHandleElement: [
          { type: s.ViewChild, args: ['minHandle', { read: H }] },
        ],
        maxHandleElement: [
          { type: s.ViewChild, args: ['maxHandle', { read: H }] },
        ],
        floorLabelElement: [
          { type: s.ViewChild, args: ['floorLabel', { read: C }] },
        ],
        ceilLabelElement: [
          { type: s.ViewChild, args: ['ceilLabel', { read: C }] },
        ],
        minHandleLabelElement: [
          { type: s.ViewChild, args: ['minHandleLabel', { read: C }] },
        ],
        maxHandleLabelElement: [
          { type: s.ViewChild, args: ['maxHandleLabel', { read: C }] },
        ],
        combinedLabelElement: [
          { type: s.ViewChild, args: ['combinedLabel', { read: C }] },
        ],
        ticksElement: [
          { type: s.ViewChild, args: ['ticksElement', { read: L }] },
        ],
        tooltipTemplate: [{ type: s.ContentChild, args: ['tooltipTemplate'] }],
        sliderElementVerticalClass: [
          { type: s.HostBinding, args: ['class.vertical'] },
        ],
        sliderElementAnimateClass: [
          { type: s.HostBinding, args: ['class.animate'] },
        ],
        sliderElementWithLegendClass: [
          { type: s.HostBinding, args: ['class.with-legend'] },
        ],
        sliderElementDisabledAttr: [
          { type: s.HostBinding, args: ['attr.disabled'] },
        ],
        sliderElementAriaLabel: [
          { type: s.HostBinding, args: ['attr.aria-label'] },
        ],
        onResize: [
          { type: s.HostListener, args: ['window:resize', ['$event']] },
        ],
      }),
      z);
  function z(e, i, t, n) {
    (this.renderer = e),
      (this.elementRef = i),
      (this.changeDetectionRef = t),
      (this.zone = n),
      (this.value = null),
      (this.valueChange = new s.EventEmitter()),
      (this.highValue = null),
      (this.highValueChange = new s.EventEmitter()),
      (this.options = new p()),
      (this.userChangeStart = new s.EventEmitter()),
      (this.userChange = new s.EventEmitter()),
      (this.userChangeEnd = new s.EventEmitter()),
      (this.initHasRun = !1),
      (this.inputModelChangeSubject = new l.Subject()),
      (this.inputModelChangeSubscription = null),
      (this.outputModelChangeSubject = new l.Subject()),
      (this.outputModelChangeSubscription = null),
      (this.viewLowValue = null),
      (this.viewHighValue = null),
      (this.viewOptions = new p()),
      (this.handleHalfDimension = 0),
      (this.maxHandlePosition = 0),
      (this.currentTrackingPointer = null),
      (this.currentFocusPointer = null),
      (this.firstKeyDown = !1),
      (this.touchId = null),
      (this.dragging = new k()),
      (this.sliderElementVerticalClass = !1),
      (this.sliderElementAnimateClass = !1),
      (this.sliderElementWithLegendClass = !1),
      (this.sliderElementDisabledAttr = null),
      (this.sliderElementAriaLabel = 'ngx-slider'),
      (this.barStyle = {}),
      (this.minPointerStyle = {}),
      (this.maxPointerStyle = {}),
      (this.fullBarTransparentClass = !1),
      (this.selectionBarDraggableClass = !1),
      (this.ticksUnderValuesClass = !1),
      (this.intermediateTicks = !1),
      (this.ticks = []),
      (this.eventListenerHelper = null),
      (this.onMoveEventListener = null),
      (this.onEndEventListener = null),
      (this.moving = !1),
      (this.resizeObserver = null),
      (this.onTouchedCallback = null),
      (this.onChangeCallback = null),
      (this.eventListenerHelper = new x(this.renderer));
  }
  (G.decorators = [
    {
      type: s.Component,
      args: [
        {
          selector: 'ngx-slider-tooltip-wrapper',
          template:
            '<ng-container *ngIf="template">\n  <ng-template *ngTemplateOutlet="template; context: {tooltip: tooltip, placement: placement, content: content}"></ng-template>\n</ng-container>\n\n<ng-container *ngIf="!template">\n  <div class="ngx-slider-inner-tooltip" [attr.title]="tooltip" [attr.data-tooltip-placement]="placement">\n    {{content}}\n  </div>\n</ng-container>',
          styles: ['.ngx-slider-inner-tooltip{height:100%}'],
        },
      ],
    },
  ]),
    (G.propDecorators = {
      template: [{ type: s.Input }],
      tooltip: [{ type: s.Input }],
      placement: [{ type: s.Input }],
      content: [{ type: s.Input }],
    }),
    (i = G);
  function G() {}
  (K.decorators = [
    {
      type: s.NgModule,
      args: [
        {
          imports: [t.CommonModule],
          declarations: [_, L, H, C, i],
          exports: [_],
        },
      ],
    },
  ]),
    (t = K);
  function K() {}
  (e.NgxSliderModule = t),
    (e.ChangeContext = c),
    (e.PointerType = d),
    (e.LabelType = u),
    (e.Options = p),
    (e.ɵb = L),
    (e.ɵc = H),
    (e.ɵd = C),
    (e.ɵa = _),
    (e.ɵe = i),
    Object.defineProperty(e, '__esModule', { value: !0 });
});
//# sourceMappingURL=angular-slider-ngx-slider.umd.min.js.map
