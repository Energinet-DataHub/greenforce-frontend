{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "dh-library-generator",
  "type": "object",
  "properties": {
    "libraryType": {
      "description": "Select a library type",
      "type": "string",
      "enum": ["feature", "data-access"],
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Select a library type"
    },
    "name": {
      "type": "string",
      "description": "Library name (note: if the selected library type is \"feature\" or \"data-access\", the final name will be prefixed with it, e.g. `feature-<name>`)",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "What name would you like to use for the library? (note: if the selected library type is \"feature\" or \"data-access\", the final name will be prefixed with it, e.g. `feature-<name>`)"
    },
    "domain": {
      "type": "string",
      "description": "To which domain does the library belong to?",
      "$default": {
        "$source": "argv",
        "index": 2
      },
      "x-prompt": "To which domain does the library belong to?"
    }
  },
  "required": ["libraryType", "name", "domain"]
}
