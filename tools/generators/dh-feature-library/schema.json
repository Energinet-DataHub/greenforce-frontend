{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "dh-feature-library",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Library name (note: final name is prefixed with `feature-`)",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the library? (note: final name is prefixed with `feature-`)"
    },
    "domain": {
      "type": "string",
      "description": "To which domain does the library belong to?",
      "x-prompt": "To which domain does the library belong to?"
    }
  },
  "required": ["name", "domain"]
}
