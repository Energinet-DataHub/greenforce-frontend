{
  "$id": "RemoveLicense",
  "title": "",
  "type": "object",
  "properties": {
    "pattern": {
      "type": "string",
      "description": "Glob pattern to match files to remove license from"
    }
  },
  "required": ["pattern"]
}
