{
  "$id": "PeerDependencies",
  "title": "",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "project to search for external dependencies",
      "required": true
    },
    "packageJson": {
      "type": "string",
      "description": "package.json to write peer dependencies to",
      "required": true
    }
  }
}
